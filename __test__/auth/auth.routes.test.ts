import {describe, test, jest} from "@jest/globals"

describe("authentification", () => {
    test("test for login", async() => {
        // if the login success from the controller respond 201 
        //const result = jest.spyOn(authController,"login")
        // else 400
    })
    test("test for logOut",() => {
        //if the logout success from the controller respond 200 or 301 
    })

    test("test for modifying user information", () => {
        // if the client modified their information from controller, repond 200
        // else  400
    })
})