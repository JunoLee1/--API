# 2026-07-18 작업 로그

## 브랜치

`feat/squad-planner`

---

## 버그 수정

### 전술 분석 — HEAD_COACH 403 오류

| 항목 | 내용 |
|------|------|
| 파일 | `apps/api/src/auth/auth.controller.ts` |
| 원인 | `refresh` 엔드포인트가 토큰 재발급 시 `coachingRole` / `frontOfficeRole` 누락 |
| 수정 | `req.user!` 전체를 `generateTokens`에 전달하도록 변경 |

### 경기 목록 셀렉트 렌더링 오류

| 항목 | 내용 |
|------|------|
| 파일 | `football/src/pages/matches/MatchesPage.tsx` |
| 증상 | 시즌 셀렉트에 ID 숫자, 대회 셀렉트에 영문 enum 값 표시 |
| 원인 | base-ui `Select.Root`에 `items` prop 미전달 — `Select.Value`가 레이블 해석 불가 |
| 수정 | 시즌·대회·다이얼로그 내 셀렉트 전체에 `items` Record 맵 전달 |

### 알림 무음 실패

| 항목 | 내용 |
|------|------|
| 파일 | `apps/api/prisma/schema.prisma` |
| 증상 | 알림 페이지에 아무것도 표시되지 않음 |
| 원인 | `INJURY_OCCURRED` 등 7개 타입이 `NotificationType` enum에 없어 PostgreSQL이 INSERT를 거부하고, `try/catch`에서 조용히 삼켜짐 |
| 수정 | 누락된 enum 값 추가 후 `prisma db push` 및 클라이언트 재생성 |

**추가된 enum 값:**
`INJURY_OCCURRED` · `INJURY_RETURNED` · `SQUAD_DEPTH_LOW` · `PLAYER_CONTRACT_SIGNED` · `COACH_SHORTLISTED` · `COACH_APPROVAL_PENDING` · `ATTENDANCE_PENALTY`

---

## 신규 기능

### 팀 빌더 — 포지션별 가용 인원 경고

| 파일 | 내용 |
|------|------|
| `football/src/pages/squad/SquadPlannerPage.tsx` | 헤더에 빨간 경고 배지 표시. 기준 미달 포지션 존 감지 (GK ≥2 / DEF ≥4 / MID ≥3 / FWD ≥2) |

### 부상 알림 — 코칭스태프 & 수석코치

| 파일 | 내용 |
|------|------|
| `apps/api/src/notification/notification.repo.ts` | `createForCoachingStaff()` 메서드 추가 — `COACHING_STAFF` 역할 전원에게 알림 생성 |
| `apps/api/src/injury/injury.repo.ts` | `getPlayerName()` · `countAvailableByZone()` 추가 — 포지션 존별 가용 인원 집계 |
| `apps/api/src/injury/injury.service.ts` | `createInjury` / `updateStatus` 알림 발송 확장, `checkAndNotifySquadDepth()` 추가 |

**알림 트리거:**

| 이벤트 | 수신자 | 타입 |
|--------|--------|------|
| 부상 발생 | 전 코칭스태프 | `INJURY_OCCURRED` |
| 복귀 준비 (`READY_TO_RETURN`) | 전 코칭스태프 | `INJURY_READY_TO_RETURN` |
| 복귀 완료 (`RETURNED`) | 전 코칭스태프 | `INJURY_RETURNED` |
| 포지션 존 가용 인원 부족 | HEAD_COACH | `SQUAD_DEPTH_LOW` |

모든 알림은 Socket.IO `staff-room` 채널로 실시간 emit 병행.

---

## 커밋 목록

| 해시 | 메시지 |
|------|--------|
| `3b08036` | fix(auth): preserve coachingRole and frontOfficeRole on token refresh |
| `21fd0c3` | fix(matches): fix season showing ID and competition showing English in select triggers |
| `01571d7` | feat(squad): add squad depth warnings per position zone in team builder |
| `c93b2b5` | feat(injury): notify coaching staff on injury events, head coach on squad depth |
| `8e125fb` | fix(notification): add missing NotificationType enum values |

---

## PR #28 — 훈련 결과 조회 탭 + TRAINING ���고서 타입

### 브랜치

`feat/training-results-report-type`

### 버그 수정

#### 훈련 등록 Select 렌더링 오류

| 항목 | 내용 |
|------|------|
| 파일 | `football/src/pages/training/TrainingPage.tsx` |
| 증상 | 시즌 셀렉트에 ID, 세션유형에 영문 enum 표시 |
| 원인 | base-ui `Select.Root`에 `items` prop 미전달 |
| 수정 | 세션유형·시즌(��이얼로그 + 필���) 전체 `items` 맵 전달 |

#### 훈련 결과 중복 생성

| 항목 | 내용 |
|------|------|
| 파일 | `apps/api/prisma/schema.prisma`, `training.repo.ts`, `training.service.ts` |
| 증상 | 동일 세션·선수 결과가 중복 저장 |
| 원인 | `TrainingResult`에 `@@unique([sessionId, playerId])` 제약 없음 + `findFirst → create` race condition |
| 수정 | unique 제약 추가, Prisma native `upsert` 교체, 기존 중복 80건 정리 |

### 신규 기능

#### 훈련 결과 조회 탭

| 파일 | 내용 |
|------|------|
| `apps/api/src/training/training.repo.ts` | `findResults()` — 날짜·��션유형·선수 필터 |
| `apps/api/src/training/training.service.ts` | `getResults()` ���가 |
| `apps/api/src/training/training.controller.ts` | `getResults` 핸들러 |
| `apps/api/src/training/training.routes.ts` | `GET /results` (`/:id` 앞에 등록) |
| `football/src/types/training.ts` | `TrainingResultRow`, `TrainingResultFilters` 타입 |
| `football/src/services/training.service.ts` | `trainingApi.getResults()` |
| `football/src/pages/training/TrainingResultsPage.tsx` | 조회 페이지 + CSV 내보내기 (UTF-8 BOM) |
| `football/src/App.tsx` | `/training/results` 라우트 |
| `football/src/layouts/AppShell.tsx` | 사이드바 "훈련 결과" 링크 |

#### TRAINING 보고서 타입 + HEAD_COACH 결��

| 파일 | 내용 |
|------|------|
| `apps/api/prisma/schema.prisma` | `ReportType` enum�� `TRAINING` 추가 |
| `apps/api/src/report/report.controller.ts` | `isHeadCoach()` 헬퍼, approve/reject/list 분기 |
| `apps/api/src/report/report.service.ts` | `list(isHeadCoach)` 파라미터 추가 |
| `apps/api/src/report/report.repo.ts` | HEAD_COACH → TRAINING 보고서 열람 분기 |
| `football/src/types/report.ts` | `TRAINING` 타입·레이블(`훈련`)·스���일(blue) |

**��재 규칙:** TRAINING 타입 → HEAD_COACH 승인/반려, 나머지 → GM

#### 출석현황 환산결석 컬럼

| 파일 | 내용 |
|------|------|
| `football/src/pages/training/TrainingAttendancePage.tsx` | 환산결석 = 무����석 + floor(무단지각 / 3) 컬럼 추가 |

#### 훈련 세션 등록 알림

| 파일 | 내용 |
|------|------|
| `apps/api/src/training/training.service.ts` | 세션 등록 시 HEAD_COACH에게 `TRAINING_SESSION_PENDING` 알림 발송 |
| `apps/api/prisma/schema.prisma` | `NotificationType` enum에 `TRAINING_SESSION_PENDING` 추가 |

#### CONTEXT.md 도메인 정의 추가

`PlayerCallup` (유소년 콜업 워크플로우), `TrainingVideo`, `VideoAssignment` 도메인 용어 정의

### 커밋 목록

| 해시 | 메시지 |
|------|--------|
| `0c82e50` | fix(training): fix select rendering and add HEAD_COACH notification on session create |
| `5bbfe86` | feat(training): add findResults repo method with date/type/player filters |
| `d698ece` | feat(training): GET /training/results with date/sessionType/player filters |
| `118ca9e` | feat(training): add TrainingResultRow type and getResults API call |
| `58b5de9` | feat(training): add TrainingResultsPage with date filter and CSV export |
| `673504b` | feat(training): register /training/results route and sidebar link |
| `7b1ba53` | feat(report): add TRAINING to ReportType enum |
| `86d1a12` | feat(attendance): add 환산결석 column (무단결석 + floor(무단지각/3)) |
| `de94faa` | feat(report): HEAD_COACH can approve/view TRAINING reports |
| `e597ff1` | feat(report): add TRAINING report type with label and style |
| `6b55be6` | fix(training): prevent duplicate results with unique constraint + Prisma upsert |
| `c0294e4` | docs(context): add PlayerCallup, TrainingVideo, VideoAssignment domain definitions |
