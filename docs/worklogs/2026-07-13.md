# 2026-07-13 작업 로그

## PR 목록

| PR | 제목 | 상태 |
|----|------|------|
| [#11](https://github.com/JunoLee1/--API/pull/11) | Issues #5 #6 #7 #10 — Equipment, Prospect, Role Corrections | MERGED |
| [#12](https://github.com/JunoLee1/--API/pull/12) | 사용자 관리 UI | MERGED |
| [#13](https://github.com/JunoLee1/--API/pull/13) | 사용자 관리 API | MERGED |
| [#14](https://github.com/JunoLee1/--API/pull/14) | Equipment·Prospect·Tactical·Notifications·Me 페이지 + .gitignore | MERGED |
| [#15](https://github.com/JunoLee1/--API/pull/15) | Player CRUD, 쿠키 인증 수정, 빠른 로그인 (선수 포함) | OPEN |

---

## PR #11 — 백엔드 도메인 모듈 (Issues #5 #6 #7 #10)

### Equipment 모듈 (Issue #5)

| 파일 | 내용 |
|------|------|
| `schema.prisma` | `Equipment`, `EquipmentAssignment`, `EquipmentCategory`, `EquipmentStatus` 추가 |
| `equipment/equipment.dto.ts` | Create·Update·AssignEquipment DTO |
| `equipment/equipment.repo.ts` | 7개 메서드 (findAll/findById/create/update/assign/unassign/countByStatus) |
| `equipment/equipment.service.ts` | 재고 부족 시 알림 생성 로직 포함 |
| `equipment/equipment.controller.ts` | 역할별 접근 제어 (ADMIN·EQUIPMENT_MANAGER·FRONT_OFFICE) |
| `equipment/equipment.routes.ts` | CRUD + 배정·반납 엔드포인트 |

### Prospect 모듈 (Issue #6)

| 파일 | 내용 |
|------|------|
| `schema.prisma` | `Prospect`, `ProspectStatus` enum 추가 |
| `prospect/prospect.dto.ts` | Create·Update·UpdateStatus DTO |
| `prospect/prospect.repo.ts` | 5개 메서드 |
| `prospect/prospect.service.ts` | 상태 머신: `ACTIVE → SIGNED \| ARCHIVED` (SIGNED는 `convertedPlayerId` 필수) |
| `prospect/prospect.controller.ts` | 읽기: ADMIN·FRONT_OFFICE·HEAD_COACH / 쓰기: ADMIN·SCOUT·GM |
| `prospect/prospect.routes.ts` | CRUD + 상태 변경 엔드포인트 |

### 역할 보정 (Issue #7 #10)

- `injury` — MEDICAL_DIRECTOR GET /stats 엔드포인트 추가
- `tactical` — TACTICAL_ANALYST DRAFT 생성, HEAD_COACH 확정
- `training` — HEAD_COACH 세션 승인
- `match` — CompetitionType enum 정리, Venue 필드 추가
- `schema.prisma` — Transfer·Notification 기반 스키마 확정

---

## PR #12 #13 — 사용자 관리

### API (PR #13)

| 엔드포인트 | 권한 | 설명 |
|-----------|------|------|
| `GET /admin/users` | ADMIN | 목록 (role·status 필터, 검색) |
| `GET /admin/users/:id` | ADMIN | 상세 |
| `PATCH /admin/users/:id/role` | ADMIN | 역할·코칭역할·FO역할 변경 |
| `PATCH /admin/users/:id/deactivate` | ADMIN | 비활성화 |
| `PATCH /admin/users/:id/reactivate` | ADMIN | 재활성화 |
| `DELETE /admin/users/:id` | ADMIN | 삭제 |

### UI (PR #12)

| 파일 | 내용 |
|------|------|
| `admin/UsersPage.tsx` | 사용자 목록 (역할·상태 필터, 이름 검색), 역할 변경·비활성화·삭제 |
| `admin/UserEditDialog.tsx` | 역할·서브역할 변경 다이얼로그 |

---

## PR #14 — FE 페이지 보완 + .gitignore

| 파일 | 내용 |
|------|------|
| `equipment/EquipmentPage.tsx` | 장비 목록·등록·배정, 재고 현황 |
| `prospects/ProspectsPage.tsx` | 유망주 목록·등록·수정·상태변경 |
| `tactical/TacticalPage.tsx` | 전술 분석 목록 |
| `notifications/NotificationsPage.tsx` | 알림 목록 |
| `me/MePage.tsx` | 내 정보 조회 |
| `.gitignore` | `.claude/`, `docs/`, `graphify-out/`, `node_modules/` |

---

## PR #15 — Player CRUD 완성 + 인증 수정 (OPEN)

### 백엔드

| 항목 | 내용 |
|------|------|
| `DELETE /players/:id` | ADMIN 전용, 204 No Content |
| `GET /countries` | `apiRouter.ts` 누락 등록 수정 |
| `country.controller.ts` | `this` 컨텍스트 유실 → 화살표 함수 변환, `undefined.toUpperCase()` 가드 추가 |

### 프런트엔드

| 항목 | 내용 |
|------|------|
| `api.ts` | 쿠키 기반 인증 전환 (`credentials: 'include'`) |
| `auth.service.ts` | 로그인 후 `localStorage.loggedIn` 플래그, `me()` → `/auth/me` |
| `vite.config.ts` | 프록시 포트 5001 → 3001 수정 |
| `PlayerDetailPage.tsx` | 삭제 버튼 + 확인 다이얼로그 (ADMIN 전용) |
| `PlayerFormDialog.tsx` | `/countries` 응답 `{ data: Country[] }` 래퍼 처리 |
| `LoginPage.tsx` | 빠른 로그인 버튼 4개 (관리자·코칭스태프·프런트오피스·**선수**) |
| `seed.ts` | `player@club.com` 계정 추가 (비밀번호: `Password1!`) |

---

## 버그 픽스

| 증상 | 원인 | 수정 |
|------|------|------|
| Nav 미표시 | 쿠키 인증인데 FE가 응답 바디에서 토큰 읽으려 함 | 인증 흐름 전면 재작성 |
| 국적 드롭다운 빈칸 | `/api/countries` 라우트 미등록 | `apiRouter.ts`에 등록 |
| 선수 등록 폼 오류 | `/countries` 응답이 `{ data: [] }` 래퍼인데 `[]`로 처리 | 타입 가드 추가 |
| `[PARSE_ERROR]` 빌드 오류 | `auth.ts` 내 머지 충돌 마커 잔존 | 파일 재작성 |
| API 응답 없음 | `.env`의 PORT가 5432(PostgreSQL) | 3001로 변경 |

---

## 시드 계정

| 이메일 | 역할 | 비밀번호 |
|--------|------|---------|
| admin@club.com | ADMIN | Password1! |
| coach@club.com | COACHING_STAFF / HEAD_COACH | Password1! |
| fo@club.com | FRONT_OFFICE / SCOUT | Password1! |
| player@club.com | PLAYER | Password1! |
