# 2026-07-14 작업 로그

## PR 목록

| PR | 제목 | 상태 |
|----|------|------|
| [#16](https://github.com/JunoLee1/--API/pull/16) | feat: 인수인계서 §4·§7·§8 — Permission 분리 / 팀 순위 / 분석 UI 개선 | OPEN |

---

## PR #16 — 인수인계서 §4·§7·§8 구현

### §4 부서별 권한 체계 및 결재 프로세스 (Admin-Finance 겸임 구조)

| 파일 | 내용 |
|------|------|
| `src/lib/permissions.ts` | `Permission` enum 신규: `SYSTEM_MANAGE` / `FINANCE_APPROVE` / `VIEW_TEAM_RANKING`. `hasPermission(role, permission)` 유틸리티 |
| `src/lib/auditLog.ts` | `writeAuditLog()` 헬퍼 — actorId·action·targetId·detail 기록 |
| `prisma/migrations/…_add_audit_log/` | AuditLog 테이블 마이그레이션 (schema.prisma는 이미 선언되어 있었음) |
| `src/admin/admin.controller.ts` | `requireAdmin` → `hasPermission(SYSTEM_MANAGE)` 전환. `updateRole` · `deactivateUser` · `reactivateUser` 성공 후 `writeAuditLog` 기록 |

**설계 의도**: 추후 Finance 전용 계정 분리 시 `ROLE_PERMISSIONS` 매핑만 수정하면 코드 수정 없이 권한 분리 가능.

---

### §7 경기/분석 섹션 — 리그/팀 순위 조회 권한 설계

#### API (BE)

| 엔드포인트 | 권한 | 설명 |
|-----------|------|------|
| `GET /api/analysis/rankings` | 전 내부 역할 (AGENT 차단) | 완료 경기 집계 → 팀별 W·D·L·GF·GA·GD·승점 계산, 승점↓·득실차↓·득점↓ 정렬 |

#### UI (FE)

| 파일 | 내용 |
|------|------|
| `src/services/analysis.service.ts` | `analysisApi.getRankings()` |
| `src/pages/matches/RankingsPage.tsx` | 시즌·대회 필터, 순위표, 우리팀 행 하이라이트 |
| `src/layouts/AppShell.tsx` | `경기·분析` 섹션 `팀 순위` 메뉴 추가 (roles 없음 → 전 역할 노출) |
| `src/App.tsx` | `/matches/rankings` 라우트 등록 (`/matches/:id` 앞에 위치) |

---

### §8 경기/분석 섹션 — 팀 단위 분석 및 선수 통계 균형 설계

| 파일 | 내용 |
|------|------|
| `src/pages/matches/MatchDetailPage.tsx` | 팀 스탯 → 인포그래픽 카드 재설계 (점유율·슈팅/유효슈팅·패스성공률·xG 대형 카드 + 코너킥·파울·경고 등 보조 타일). 선수 스탯 테이블에 슈팅 팀 기여도(%) 배지 추가 (팀 성적 → 개인 지표 탑다운 흐름) |

---

## 버그 수정

| 증상 | 원인 | 수정 |
|------|------|------|
| 관리자 경기 섹션 미표시 | LNB `경기·分析`(漢字 析, U+6790) vs SECTION_ORDER `경기·분석`(한글 석, U+C11D) 불일치 | nav items section 문자열 한글로 통일 |
| 선수 등록 국적 드롭다운 2개뿐 | `country.service.getCountries`가 DB에 결과 있으면 외부 API 미호출, 시드에 국가 2개뿐 | DB 국가 수 < 50 시 `restcountries.com` 자동 동기화 후 반환 |

---

## 시드 및 개발 편의

| 항목 | 내용 |
|------|------|
| `prisma/seed.ts` | 코칭스태프 전 역할 계정 8개 추가 (assistant·defensive·attacking·physical·setpiece·gk·medical·meddir @club.com). `fo@club.com` frontOfficeRole `SCOUT` 설정 (누락 수정) |
| `src/pages/auth/LoginPage.tsx` | 빠른 로그인 버튼 그룹화: 기본(관리자·프런트·선수) + 코칭스태프 9개 역할 |

---

## 시드 계정 전체 목록 (pw: Password1!)

| 이메일 | 역할 | 서브역할 |
|--------|------|---------|
| admin@club.com | ADMIN | — |
| fo@club.com | FRONT_OFFICE | SCOUT |
| player@club.com | PLAYER | — |
| coach@club.com | COACHING_STAFF | HEAD_COACH |
| assistant@club.com | COACHING_STAFF | ASSISTANT_COACH |
| defensive@club.com | COACHING_STAFF | DEFENSIVE_COACH |
| attacking@club.com | COACHING_STAFF | ATTACKING_COACH |
| physical@club.com | COACHING_STAFF | PHYSICAL_COACH |
| setpiece@club.com | COACHING_STAFF | SET_PIECE_COACH |
| gk@club.com | COACHING_STAFF | GOALKEEPER_COACH |
| medical@club.com | COACHING_STAFF | MEDICAL |
| meddir@club.com | COACHING_STAFF | MEDICAL_DIRECTOR |

---

## 그래프 업데이트

- graphify --update 실행: 2,683 nodes · 3,029 edges · 76 communities
- 주요 God Nodes: `EquipmentService` (degree 23) · `EquipmentRepository` (22) · `PrismaClient` (20) · `DashboardRepository` (19) · `Role` (16)
- `graphify-out/graph.html` 갱신됨
