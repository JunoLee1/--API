generator client {
  provider = "prisma-client"
  output   = "../src/generated"
}

datasource db {
  provider = "postgresql"
}

// ─────────────────────────────────────────────
// Enums
// ─────────────────────────────────────────────

enum Role {
  ADMIN
  SUPER_ADMIN
  GM
  FRONT_OFFICE
  COACHING_STAFF
  PLAYER
  AGENT
  GUARDIAN
}

enum YouthRegistrationStatus {
  PENDING
  GUARDIAN_APPROVED
  CONTRACTED
  REJECTED
}

enum IncidentReportStatus {
  DRAFT
  SUBMITTED
  SIGNED
}

enum IncidentType {
  MATCH
  TRAINING
}

enum CoachingRole {
  HEAD_COACH
  ASSISTANT_COACH
  DEFENSIVE_COACH
  ATTACKING_COACH
  PHYSICAL_COACH
  SET_PIECE_COACH
  GOALKEEPER_COACH
  MEDICAL
  MEDICAL_DIRECTOR
}

enum FrontOfficeRole {
  TD
  CONTRACT_MANAGER
  SCOUT
  EQUIPMENT_MANAGER
  TACTICAL_ANALYST
  FINANCE_MANAGER
  ASSET_MANAGER
  HR_MANAGER
  FACILITY_MANAGER
  HR_STAFF
  ASSET_STAFF
  FINANCE_STAFF
  FACILITY_STAFF
}

enum PlayerLevel {
  YOUTH
  ROOKIE
  SENIOR
  VETERAN
}

enum PlayerStatus {
  ACTIVE
  ON_LOAN
  RELEASED
  RETIRED
}

enum Position {
  GOALKEEPER
  STRIKER
  SHADOW_STRIKER
  WINGER
  CENTRAL_ATTACK_MIDFIELDER
  RIGHT_ATTACK_MIDFIELDER
  LEFT_ATTACK_MIDFIELDER
  CENTRAL_DEFENSIVE_MIDFIELDER
  LEFT_DEFENSIVE_MIDFIELDER
  RIGHT_DEFENSIVE_MIDFIELDER
  CENTER_BACK
  LEFT_WING_BACK
  LEFT_FULL_BACK
  RIGHT_WING_BACK
  RIGHT_FULL_BACK
}

enum Foot {
  LEFT
  RIGHT
  BOTH
}

enum ContractStatus {
  ACTIVE
  EXPIRED
  TERMINATED
}

enum BonusMetric {
  GOALS
  ASSISTS
  APPEARANCES
  CLEAN_SHEETS
  SAVES
  PASS_ACCURACY
  TACKLE_SUCCESS_RATE
  CLEARANCES
  INTERCEPTIONS
  XG
  TEAM_RANK
  TEAM_WINS
}

enum BonusPeriod {
  SEASON
  MONTH
  MATCH
}

enum TransferType {
  PERMANENT_IN
  PERMANENT_OUT
  LOAN_OUT
  LOAN_IN
  FREE
  RELEASE
}

enum TransferRequestStatus {
  DRAFT
  PENDING_APPROVAL
  APPROVED
  CONFIRMED
  REJECTED
}

enum RecallStatus {
  PENDING
  APPROVED
  REJECTED
}

enum BodyPart {
  HEAD_FACE
  NECK_SHOULDER
  TORSO_BACK
  THIGH_FRONT
  THIGH_BACK
  KNEE
  SHIN_CALF
  ANKLE
  FOOT_TOE
  OTHER
}

enum InjuryCause {
  TRAINING
  MATCH
  OTHER
}

enum InjuryStatus {
  OCCURRED
  DIAGNOSED
  REHABILITATING
  READY_TO_RETURN
  RETURNED
}

enum HospitalType {
  ACCREDITED
  GENERAL
}

enum PartnerType {
  MANUFACTURER
  HOSPITAL
  MAINTENANCE_VENDOR
  EQUIPMENT_SUPPLIER
}

enum PartnerContractStatus {
  ACTIVE
  EXPIRED
  TERMINATED
}

enum EquipmentLoanStatus {
  REQUESTED
  APPROVED
  REJECTED
  ISSUED
  RETURNED
}

enum DisposalVerificationStatus {
  PENDING
  FM_VERIFIED
  GM_APPROVED
  REJECTED
}

enum SessionType {
  INDIVIDUAL_SKILL
  TACTICAL_DEFENSIVE
  TACTICAL_ATTACKING
  TACTICAL_FULL_TEAM
  PHYSICAL
  PSYCHOLOGICAL_SOCIAL
  SET_PIECE
  GOALKEEPER
}

enum ContentPhase {
  WARMUP
  DRILL
  TACTICAL
  GAME
}

enum AttendanceStatus {
  PRESENT
  ABSENT_UNAUTHORIZED
  LATE_UNAUTHORIZED
  ABSENT_AUTHORIZED
  LATE_AUTHORIZED
}

enum TacticalPhase {
  PRE_MATCH
  POST_MATCH
}

enum CompetitionType {
  LEAGUE
  DOMESTIC_CUP
  CONTINENTAL
  PLAYOFF
  FRIENDLY
}

enum Venue {
  HOME
  AWAY
  NEUTRAL
}

enum NotificationType {
  CONTRACT_EXPIRY
  CONTRACT_EXPIRY_90D
  CONTRACT_EXPIRY_60D
  CONTRACT_EXPIRY_30D
  PERFORMANCE_BONUS_ACHIEVED
  INJURY_OCCURRED
  INJURY_READY_TO_RETURN
  INJURY_RETURNED
  SQUAD_DEPTH_LOW
  TRAINING_ATTENDANCE_WARNING
  EQUIPMENT_LOW_STOCK
  EXTENSION_OPTION_AVAILABLE
  EXTERNAL_REPORT_CREATED
  EXTERNAL_REPORT_DUE_SOON
  EXTERNAL_REPORT_OVERDUE
  MEDICAL_EXPENSE_SUBMITTED
  MEDICAL_EXPENSE_LEADER_APPROVED
  MEDICAL_EXPENSE_REJECTED
  MEDICAL_EXPENSE_APPROVED
  REPORT_SUBMITTED
  REPORT_REJECTED
  COACH_AUTO_SHORTLISTED
  COACH_MANUALLY_SHORTLISTED
  COACH_SHORTLISTED
  COACH_APPROVAL_REQUESTED
  COACH_APPROVAL_PENDING
  COACH_CONTRACTED
  COACH_HEAD_CONTRACTED
  COACH_ARCHIVED
  COACH_TUTOR_SUPPORT_NEEDED
  TRAINING_LOAD_ALERT
  PLAYER_DEVELOPMENT_PLAN_ACTIVATED
  PLAYER_CONTRACT_SIGNED
  ATTENDANCE_PENALTY
  TRAINING_SESSION_PENDING
  VIDEO_ASSIGNED
  VIDEO_ASSIGNMENT_OVERDUE
  WORK_PERMIT_EXPIRY_SOON
  WORK_PERMIT_EXPIRY_SOON_60D
  CALLUP_REQUESTED
  CALLUP_APPROVED
  CALLUP_REJECTED
  CALLUP_DOCS_READY
  LOAN_OUT_EXPIRED
  TACTICAL_ANALYSIS_CONFIRM_REQUESTED
  JERSEY_NUMBER_CONFLICT
  MATCH_DAY_REMINDER
  ATTENDANCE_UNAUTHORIZED
  ATTENDANCE_PENALTY_PLAYER
  YOUTH_REGISTRATION_STATUS_CHANGED
  YOUTH_WEEKLY_SCHEDULE
  YOUTH_SESSION_CHANGED
  GUARDIAN_CHILD_INJURY
  GUARDIAN_CHILD_CALLUP
  INCIDENT_REPORT_SUBMITTED
  GROWTH_REPORT_PUBLISHED
  SAFEGUARD_EMERGENCY
  FEE_INVOICE_ISSUED
  FEE_REMINDER
  FEE_OVERDUE_WARNING
  FEE_ACCOUNT_LOCKED
  FACILITY_EMERGENCY
  FACILITY_MAINTENANCE_RESOLVED
  EQUIPMENT_INSPECTION_DUE
  PAYROLL_CONFIRMED
  JOB_POSTING_DRAFT_CREATED
  PAYROLL_SECOND_APPROVED
  IT_ASSET_EXPIRY_SOON
  IT_ASSET_RETIREMENT_SYNC
  INVENTORY_LOW_STOCK
  FINANCE_SUBMIT_REQUIRED
  SALES_NEGATIVE_VALUE
  LICENSE_SEAT_EXCEEDED
  INTERVIEW_SCHEDULED
  COMPLIANCE_DEADLINE_REMINDER
  EQUIPMENT_RETURN_OVERDUE
  SPONSORSHIP_EXPIRY_90D
  SPONSORSHIP_EXPIRY_60D
  SPONSORSHIP_EXPIRY_30D
  HIRING_SURVEY_OPEN
  HIRING_SURVEY_DEADLINE_REMINDER
  HIRING_SURVEY_CLOSED
  HIRING_PLAN_APPROVED
  INJURY_REHABILITATING_STARTED
  INJURY_REPORT_UPDATED
  TRAINING_HIGH_PERFORMANCE_PLAYER
  TRAINING_HIGH_PERFORMANCE_SELF
  PARTNER_CONTACT_FOLLOWUP
}

enum DepreciationMethod {
  STRAIGHT_LINE
  DECLINING_BALANCE
}

enum CurrencyCode {
  KRW
  USD
  EUR
  GBP
}

enum LedgerEntryType {
  INCOME
  EXPENSE
}

enum AccountCodeType {
  REVENUE
  EXPENSE
}

enum RevenueField {
  TICKET
  SPONSORSHIP
  BROADCAST
  MERCHANDISE
  SUBSIDY
  PARENT_COMPANY
  ACADEMY_FEE
  OTHER
}

enum LedgerEntryCategory {
  SALARY
  EQUIPMENT_PURCHASE
  FACILITY_REPAIR
  TRANSFER_FEE
  TICKET_SALES
  UNIFORM_SALES
  SPONSORSHIP
  ACADEMY_FEE
  REFUND
  OTHER
  MERCHANDISE
}

enum SalesType {
  TICKET
  UNIFORM
  OTHER
  COMPLIMENTARY
  VIP_TICKET
}

enum BadgeType {
  PASSION_KING
  SPACE_WIZARD
  BEST_PASSER
  TEAM_PLAYER
  MOST_IMPROVED
  DEFENSIVE_WALL
  GOAL_MACHINE
}

enum TacticalStatus {
  DRAFT
  CONFIRMED
}

enum SeasonStatus {
  UPCOMING
  ACTIVE
  CLOSED
}

enum WageCapType {
  FIXED
  RATIO
}

enum EquipmentCategory {
  CLOTHING
  FOOTWEAR
  BALL_AND_TOOLS
  REHABILITATION
  TACTICAL
  OTHER
}

enum EquipmentUnitStatus {
  AVAILABLE
  IN_USE
  MAINTENANCE
  RETIRED
}

enum ProspectStatus {
  ACTIVE
  MEDICAL_TEST
  CONTRACT_PENDING
  SIGNED
  ARCHIVED
}

enum VisaEligibility {
  NOT_REQUIRED
  CONFIRMED
  UNCERTAIN
}

enum WorkPermitStatus {
  NOT_REQUIRED
  PENDING
  APPROVED
  REJECTED
}

enum TeamType {
  FIRST_TEAM
  YOUTH
  B_TEAM
}

enum CoachStatus {
  CANDIDATE
  SHORTLISTED
  APPROVAL_PENDING
  CONTRACTED
  RETIRED
  ARCHIVED
}

enum ShortlistSource {
  SYSTEM
  MANUAL
}

enum TutorType {
  INTERNAL
  EXTERNAL
}

enum HiringRoundStatus {
  OPEN
  CLOSED
  CANCELLED
}

enum LanguageProficiency {
  A1
  A2
  B1
  B2
  C1
  C2
}

enum BonusTeamScope {
  ALL
  FIRST_TEAM_ONLY
}

enum PlayerDevelopmentPlanStatus {
  DRAFT
  ACTIVE
  REVIEWED
}

enum JerseyNumberStatus {
  AVAILABLE
  OCCUPIED
  RETIRED
  RESERVED
}

enum MarketValueSource {
  MANUAL
  EXTERNAL_API
}

enum ApplicationSource {
  SARAMIN
  GLASSDOOR
  INDEED
  FACEBOOK
  DIRECT
  AGENT
  REFERRAL
  PLATFORM
  INTERNAL
}

enum LeagueLevel {
  K3
  K_LEAGUE_2
  K_LEAGUE_1
  EPL
  OTHER
}

enum Confederation {
  UEFA
  AFC
  CONMEBOL
  CONCACAF
  CAF
  OFC
}

enum DepartmentCategory {
  COMPLIANCE
  PERFORMANCE
  FINANCE
  OPERATIONS
  HR
  MARKETING
  LEGAL
  MEDIA
}

enum ContractType {
  PROFESSIONAL
  SEMI_PROFESSIONAL
}

enum FacilityZone {
  GROUND
  MECHANICAL
  STRUCTURAL
  SAFETY
  SANITATION
  OPERATIONS
  LOCKER_ROOM
  MEDICAL_ROOM
  SHOWER_ROOM
}

enum InspectionType {
  DAILY
  MONTHLY
  QUARTERLY
  ANNUAL
}

enum InspectionResult {
  OK
  ISSUE_FOUND
}

enum MaintenancePriority {
  EMERGENCY
  HIGH
  NORMAL
}

enum CertificationType {
  PLAYER_REGISTRATION
  PLAYER_CONTRACT
  PLAYER_HEALTH_CHECK
  PLAYER_HEALTH_INSURANCE
  PLAYER_FIFA_ID
  COACH_UEFA_LICENSE
  COACH_YOUTH_LICENSE
  COACH_REFEREE_TRAINING
  STAFF_DOCTOR_LICENSE
  STAFF_MEDICAL_COORDINATOR
  STAFF_PHYSIOTHERAPIST
  STAFF_SAFETY_OFFICER
  FACILITY_STADIUM_SAFETY
  FACILITY_STRUCTURAL
  FACILITY_LIGHTING
  FACILITY_MEDICAL_EQUIPMENT
  FACILITY_FIRE_ELECTRICAL
  CLUB_LICENSE
  CLUB_CORPORATE_REGISTRATION
  CLUB_FINANCIAL_AUDIT
  CLUB_LIABILITY_INSURANCE
  CLUB_YOUTH_PROGRAM
}

enum CertEntityType {
  PLAYER
  COACH
  STAFF
  FACILITY
  CLUB
}

enum CertStatus {
  DRAFT
  PENDING_REVIEW
  FM_APPROVED
  VALID
  EXPIRING_SOON
  EXPIRED
  REJECTED
  SUSPENDED
  CANCELLED
}

enum MaintenanceStatus {
  OPEN
  IN_PROGRESS
  PENDING_APPROVAL
  APPROVED
  RESOLVED
  REJECTED
}

enum SponsorType {
  TITLE
  KIT
  STADIUM_NAMING
  DIGITAL
  OTHER
}

enum PaymentSchedule {
  MONTHLY
  QUARTERLY
  ANNUAL
}

enum SponsorshipPaymentStatus {
  PENDING
  PAID
  OVERDUE
}

enum PayrollCountry {
  KR
  UK
}

enum PayrollRunStatus {
  DRAFT
  CONFIRMED
}

enum SalesRecordStatus {
  COMPLETED
  CANCELLED
  REFUNDED
}

enum SalesChannel {
  ONLINE
  ONSITE
  PARTNER
  SEASON_PASS
}

enum ExpenseStatus {
  PENDING
  FIRST_APPROVED
  APPROVED
  PAID
  REJECTED
  CANCELLED
}

// ─────────────────────────────────────────────
// Models
// ─────────────────────────────────────────────

model Club {
  id                Int      @id @default(autoincrement())
  name              String
  isActive          Boolean  @default(true)
  isLite            Boolean  @default(false)
  countryId         Int?
  ownerEmail        String?  @unique
  businessRegNumber String?
  companyNumber     String?
  vatNumber         String?
  createdAt         DateTime @default(now())

  country     Country?     @relation(fields: [countryId], references: [id])
  teams       Team[]
  users       User[]
  leagues     ClubLeague[]
  departments Department[]
}

model Team {
  id               Int      @id @default(autoincrement())
  name             String
  type             TeamType
  ageGroup         String?
  isActive         Boolean  @default(true)
  isLite           Boolean  @default(false)
  trackStats       Boolean  @default(true)
  requiresContract Boolean  @default(true)
  clubId           Int?
  createdAt        DateTime @default(now())

  club               Club?               @relation(fields: [clubId], references: [id])
  players            Player[]
  coachingStaff      User[]
  trainingSessions   TrainingSession[]
  matches            Match[]
  coaches            Coach[]
  callupsFrom        PlayerCallup[]      @relation("CallupFromTeam")
  callupsTo          PlayerCallup[]      @relation("CallupToTeam")
  jerseyNumbers      JerseyNumber[]
  youthRegistrations YouthRegistration[]
  incidentReports    IncidentReport[]
}

model Country {
  id   Int    @id @default(autoincrement())
  name String
  code String

  users   User[]
  players Player[]
  clubs   Club[]
  leagues League[]
}

model PhoneNumber {
  id        Int    @id @default(autoincrement())
  iv        String
  encrypted String

  user User?
}

model User {
  id              Int              @id @default(autoincrement())
  email           String           @unique
  password        String
  username        String
  nickname        String           @unique
  role            Role
  coachingRole    CoachingRole?
  frontOfficeRole FrontOfficeRole?
  isDeleted       Boolean          @default(false)
  isOutOfOffice   Boolean          @default(false)
  isDemo          Boolean          @default(false)
  language        String           @default("ko")
  dateOfBirth     DateTime
  phoneNumberId   Int              @unique
  nationalityId   Int
  teamId          Int?
  clubId          Int?

  phoneNumber               PhoneNumber                     @relation(fields: [phoneNumberId], references: [id])
  nationality               Country                         @relation(fields: [nationalityId], references: [id])
  player                    Player?
  recallRequests            Recall[]                        @relation("RecallRequestedBy")
  recallApprovals           Recall[]                        @relation("RecallApprovedBy")
  managedInjuries           Injury[]
  createdSessions           TrainingSession[]               @relation("TrainingSessionCreatedBy")
  approvedSessions          TrainingSession[]               @relation("TrainingSessionApprovedBy")
  tacticalAnalyses          TacticalAnalysis[]
  notifications             Notification[]
  managedContracts          Contract[]
  agentPlayers              Player[]                        @relation("AgentPlayers")
  createdProspects          Prospect[]                      @relation("ProspectCreatedBy")
  requestedLoans            EquipmentLoan[]                 @relation("LoanRequestedBy")
  approvedLoans             EquipmentLoan[]                 @relation("LoanApprovedBy")
  auditLogs                 AuditLog[]
  authoredReports           Report[]                        @relation("ReportAuthor")
  reviewedReports           Report[]                        @relation("ReportReviewer")
  firstReviewedReports      Report[]                        @relation("ReportFirstReviewer")
  secondReviewedReports     Report[]                        @relation("ReportSecondReviewer")
  submittedMedicalExpenses  MedicalExpense[]                @relation("MedicalExpenseSubmitter")
  leaderReviewedExpenses    MedicalExpense[]                @relation("MedicalExpenseLeaderReviewer")
  adminReviewedExpenses     MedicalExpense[]                @relation("MedicalExpenseAdminReviewer")
  createdInjuryReports      InjuryReport[]                  @relation("InjuryReportCreator")
  updatedInjuryReports      InjuryReport[]                  @relation("InjuryReportUpdater")
  coachSignedReports        InjuryReport[]                  @relation("InjuryReportCoachSigner")
  trainerSignedReports      InjuryReport[]                  @relation("InjuryReportTrainerSigner")
  medicalSignedReports      InjuryReport[]                  @relation("InjuryReportMedicalSigner")
  assessedInjuries          InjuryAssessment[]
  squadConfirmations        MatchSquad[]                    @relation("SquadConfirmations")
  lineupConfirmations       MatchLineup[]                   @relation("LineupConfirmations")
  team                      Team?                           @relation(fields: [teamId], references: [id])
  club                      Club?                           @relation(fields: [clubId], references: [id])
  createdHiringRounds       CoachHiringRound[]              @relation("HiringRoundCreatedBy")
  coachProfile              Coach?                          @relation("CoachUser")
  tutorAssignments          CoachTutorAssignment[]          @relation("TutorAssignments")
  scoredTrainingResults     TrainingResult[]                @relation("TrainingResultScorer")
  coachAvailabilities       CoachAvailability[]             @relation("CoachAvailabilityUser")
  createdAvailabilities     CoachAvailability[]             @relation("CoachAvailabilityCreatedBy")
  developmentPlans          PlayerDevelopmentPlan[]         @relation("PDPCoach")
  uploadedVideos            TrainingVideo[]                 @relation("VideoUploader")
  videoAssignments          VideoAssignment[]               @relation("VideoAssigner")
  trainingReferences        TrainingReference[]
  requestedCallups          PlayerCallup[]                  @relation("CallupRequestedBy")
  approvedCallups           PlayerCallup[]                  @relation("CallupApprovedBy")
  loginHistory              LoginHistory[]
  recordedMarketValues      MarketValueHistory[]
  guardianPlayers           Player[]                        @relation("GuardianPlayers")
  guardianRegistrations     YouthRegistration[]             @relation("GuardianRegistrations")
  youthRegistrations        YouthRegistration[]             @relation("YouthRegistrationRequester")
  reportedIncidents         IncidentReport[]                @relation("IncidentReporter")
  coachEvaluations          GrowthEvaluation[]              @relation("CoachEvaluations")
  awardedBadges             PlayerBadge[]                   @relation("CoachBadges")
  isSuspended               Boolean                         @default(false)
  suspendedAt               DateTime?
  safeguardReportsAccused   SafeguardReport[]               @relation("SafeguardAccused")
  guardianFees              AcademyFee[]                    @relation("GuardianFees")
  givenEvaluations          CoachingStaffEvaluation[]       @relation("EvaluationGiver")
  receivedEvaluations       CoachingStaffEvaluation[]       @relation("EvaluationReceiver")
  staffRecordsCreated       StaffRecord[]                   @relation("StaffRecordCreator")
  inspectedFacilities       FacilityInspection[]            @relation("FacilityInspector")
  maintenanceRequests       MaintenanceRequest[]            @relation("MaintenanceRequestCreator")
  createdSponsorships       Sponsorship[]                   @relation("SponsorshipCreator")
  fmApprovedMaintenances    MaintenanceRequest[]            @relation("MaintenanceFMApprover")
  gmApprovedMaintenances    MaintenanceRequest[]            @relation("MaintenanceGMApprover")
  staffSalary               StaffSalary[]                   @relation("UserSalary")
  confirmedPayrolls         PayrollRun[]                    @relation("PayrollRunConfirmer")
  secondApprovedPayrolls    PayrollRun[]                    @relation("PayrollRunSecondApprover")
  budgetOverrideLogs        BudgetOverrideLog[]             @relation("BudgetOverrideLogCreator")
  budgetOverrideReviews     BudgetOverrideLog[]             @relation("BudgetOverrideLogReviewer")
  revenueLogChanges         FinancialReportRevenueLog[]     @relation("RevenueLogChanger")
  operatingExpenses         OperatingExpense[]              @relation("OperatingExpenseCreator")
  paidExpenses              OperatingExpense[]              @relation("OperatingExpensePayer")
  firstApprovedExpenses     OperatingExpense[]              @relation("OperatingExpenseFirstApprover")
  approvedExpenses          OperatingExpense[]              @relation("OperatingExpenseApprover")
  rejectedExpenses          OperatingExpense[]              @relation("OperatingExpenseRejecter")
  cancelledExpenses         OperatingExpense[]              @relation("OperatingExpenseCanceller")
  createdPostings           JobPosting[]                    @relation("JobPostingCreator")
  approvedPostings          JobPosting[]                    @relation("JobPostingApprover")
  applicationOffers         JobApplication[]                @relation("ApplicationOffer")
  onboarding                Onboarding?                     @relation("OnboardingUser")
  headedDepartments         Department[]                    @relation("DepartmentHead")
  departmentMemberships     UserDepartment[]
  sentInvites               UserInvite[]                    @relation("InviteSender")
  issuedGuardianCodes       GuardianInviteCode[]            @relation("GuardianCodeIssuer")
  usedGuardianCodes         GuardianInviteCode[]            @relation("GuardianCodeUser")
  createdSoftwareLicenses   SoftwareLicense[]               @relation("SoftwareLicenseCreator")
  ledgerEntries             LedgerEntry[]                   @relation("LedgerEntryCreator")
  salesRecords              SalesRecord[]                   @relation("SalesRecordCreator")
  salesRecordsUpdated       SalesRecord[]                   @relation("SalesRecordUpdater")
  squadPlans                SquadPlan[]
  facilityReservations      FacilityReservation[]           @relation("FacilityReservations")
  interviewerScores         InterviewerScore[]              @relation("InterviewerScores")
  createdAppeals            AttendanceAppeal[]              @relation("AppealCreator")
  reviewedAppeals           AttendanceAppeal[]              @relation("AppealReviewer")
  transferRequestsRequested TransferRequest[]               @relation("TRRequestedBy")
  transferRequestsReviewed  TransferRequest[]               @relation("TRReviewedBy")
  transferRequestsConfirmed TransferRequest[]               @relation("TRConfirmedBy")
  transferRequestsRejected  TransferRequest[]               @relation("TRRejectedBy")
  createdPlanReports        PlanReport[]                    @relation("PlanReportCreatedBy")
  approvedPlanReports       PlanReport[]                    @relation("PlanReportApprovedBy")
  confirmedPlanReviews      PlanReview[]                    @relation("PlanReviewConfirmer")
  confirmedReportReviews    ReportReview[]                  @relation("ReportReviewConfirmer")
  createdSurveys            HiringNeedsSurvey[]             @relation("SurveyCreatedBy")
  surveyResponses           SurveyResponse[]                @relation("SurveyResponseSubmitter")
  disposedEquipmentUnits    EquipmentUnit[]                 @relation("EquipmentDisposer")
  formationSnapshots        FormationSnapshot[]             @relation("FormationSnapshotCreatedBy")
  periodLocks               LedgerPeriodLock[]              @relation("PeriodLockCreator")
  certifications            Certification[]                 @relation("CertOwner")
  certFirstApprovals        Certification[]                 @relation("CertFirstApprover")
  certSecondApprovals       Certification[]                 @relation("CertSecondApprover")
  facilityAccessLogs        FacilityAccessLog[]
  disposalRequests          EquipmentDisposalVerification[] @relation("DisposalRequestedBy")
  disposalVerifications     EquipmentDisposalVerification[] @relation("DisposalVerifiedBy")
  sponsorshipExposureEvents SponsorshipExposureEvent[]      @relation("ExposureEventCreator")
  partnerContactLogs        PartnerContactLog[]             @relation("ContactLogActor")
  budgetHeadersCreated      BudgetHeader[]                  @relation("BudgetHeaderCreator")
  budgetHeadersApproved     BudgetHeader[]                  @relation("BudgetHeaderApprover")
  adjustmentsCreated        BudgetAdjustment[]              @relation("AdjustmentCreator")
  adjustmentsApproved       BudgetAdjustment[]              @relation("AdjustmentApprover")
  settlementsCreated        MonthlySettlementReport[]       @relation("SettlementCreator")
  settlementsFirstSubmitted MonthlySettlementReport[]       @relation("SettlementFirstSubmitter")
  settlementsFirstApproved  MonthlySettlementReport[]       @relation("SettlementFirstApprover")
  settlementsApproved       MonthlySettlementReport[]       @relation("SettlementApprover")
  revenueAdjustments        RevenueAdjustment[]             @relation("AdjCreator")
  carryOverOverrides        FinancialReport[]               @relation("CarryOverOverrider")
  assetRequests             AssetRequest[]                  @relation("AssetRequestRequester")
  assetRequestApprovals     AssetRequestApproval[]          @relation("AssetRequestReviewer")
}

model Season {
  id           Int          @id @default(autoincrement())
  name         String
  startDate    DateTime
  endDate      DateTime
  status       SeasonStatus @default(UPCOMING)
  wageCapType  WageCapType?
  wageCapValue Float?
  leagueLevel  LeagueLevel?
  leagueId     Int?

  league                     League?                     @relation(fields: [leagueId], references: [id])
  matches                    Match[]
  trainingSessions           TrainingSession[]
  tacticalAnalyses           TacticalAnalysis[]
  developmentPlans           PlayerDevelopmentPlan[]
  financialReport            FinancialReport?
  budgetHeaders              BudgetHeader[]
  operatingExpenses          OperatingExpense[]
  complianceCheck            SeasonComplianceCheck?
  monthlyBudgetSnapshots     MonthlyBudgetSnapshot[]
  monthlyOperationsSnapshots MonthlyOperationsSnapshot[]
  monthlySettlements         MonthlySettlementReport[]
  squadPlan                  SquadPlan?
}

model League {
  id            Int            @id @default(autoincrement())
  name          String
  level         LeagueLevel
  year          Int
  isActive      Boolean        @default(true)
  countryId     Int?
  confederation Confederation?
  createdAt     DateTime       @default(now())

  country Country?     @relation(fields: [countryId], references: [id])
  clubs   ClubLeague[]
  seasons Season[]

  @@unique([level, year])
}

model ClubLeague {
  clubId   Int
  leagueId Int
  joinedAt DateTime @default(now())

  club   Club   @relation(fields: [clubId], references: [id])
  league League @relation(fields: [leagueId], references: [id])

  @@id([clubId, leagueId])
}

model Player {
  id                                String           @id @default(uuid())
  playerName                        String
  preferredFoot                     Foot
  height                            Int
  weight                            Int
  position                          Position
  level                             PlayerLevel
  status                            PlayerStatus     @default(ACTIVE)
  externalId                        String?          @unique
  nationalityId                     Int
  userId                            Int?             @unique
  agentId                           Int?
  guardianId                        Int?
  agencyId                          Int?
  emergencyContactNameEncrypted     String?
  emergencyContactNameIv            String?
  emergencyContactPhoneEncrypted    String?
  emergencyContactPhoneIv           String?
  emergencyContactRelationEncrypted String?
  emergencyContactRelationIv        String?
  dateOfBirthEncrypted              String?
  dateOfBirthIv                     String?
  workPermitStatus                  WorkPermitStatus @default(NOT_REQUIRED)
  workPermitExpiry                  DateTime?
  teamId                            Int?
  promotedFromYouthAt               DateTime?
  youthOriginTeamId                 Int?

  nationality            Country                   @relation(fields: [nationalityId], references: [id])
  user                   User?                     @relation(fields: [userId], references: [id])
  agent                  User?                     @relation("AgentPlayers", fields: [agentId], references: [id])
  guardian               User?                     @relation("GuardianPlayers", fields: [guardianId], references: [id])
  agency                 Agency?                   @relation(fields: [agencyId], references: [id])
  team                   Team?                     @relation(fields: [teamId], references: [id])
  contracts              Contract[]
  transfers              Transfer[]
  injuries               Injury[]
  trainingParticipations TrainingParticipant[]
  trainingResults        TrainingResult[]
  matchStats             PlayerMatchStats[]
  tacticalLineups        TacticalLineup[]
  equipmentAssignments   EquipmentAssignment[]
  prospects              Prospect[]                @relation("ProspectConvertedPlayer")
  medicalExpenses        MedicalExpense[]
  trainingLoads          TrainingLoad[]
  developmentPlans       PlayerDevelopmentPlan[]
  momAnalyses            TacticalAnalysis[]        @relation("MomPlayer")
  improvementAnalyses    TacticalAnalysis[]        @relation("ImprovementPlayer")
  videoAssignments       VideoAssignment[]
  callups                PlayerCallup[]
  workPermitAlerts       WorkPermitAlertLog[]
  playStyle              String?
  allergies              String[]
  foodPreferences        String?
  // 최신값 캐시 — MarketValueHistory 항목 생성 시 항상 동시 업데이트할 것 (service 레이어에서 트랜잭션 보장)
  currentMarketValue     Float?
  jerseyNumbers          JerseyNumber[]
  marketValueHistory     MarketValueHistory[]
  matchSquads            MatchSquad[]
  lineupSlots            LineupSlot[]
  shotsAsShooter         ShotEvent[]               @relation("ShotShooter")
  shotsAsAssister        ShotEvent[]               @relation("ShotAssister")
  incidentReports        IncidentReport[]
  growthEvaluations      GrowthEvaluation[]
  badges                 PlayerBadge[]
  academyFees            AcademyFee[]
  secondaryPositions     PlayerSecondaryPosition[]
  studentCode            String?                   @unique
  guardianInviteCodes    GuardianInviteCode[]
  transferRequests       TransferRequest[]
  certifications         Certification[]           @relation("PlayerCertifications")
}

model Contract {
  id               Int            @id @default(autoincrement())
  startDate        DateTime
  endDate          DateTime
  salary           Int
  status           ContractStatus @default(ACTIVE)
  contractType     ContractType   @default(PROFESSIONAL)
  playerId         String
  managedById      Int?
  agencyId         Int?
  agencyCommission Decimal?       @db.Decimal(12, 2)

  player             Player             @relation(fields: [playerId], references: [id])
  managedBy          User?              @relation(fields: [managedById], references: [id])
  agency             Agency?            @relation(fields: [agencyId], references: [id])
  buyoutClause       BuyoutClause?
  extensionOptions   ExtensionOption[]
  performanceBonuses PerformanceBonus[]
}

model BuyoutClause {
  id         Int    @id @default(autoincrement())
  amount     BigInt
  contractId Int    @unique

  contract Contract @relation(fields: [contractId], references: [id])
}

model ExtensionOption {
  id             Int    @id @default(autoincrement())
  condition      String
  durationMonths Int
  contractId     Int

  contract Contract @relation(fields: [contractId], references: [id])
}

model PerformanceBonus {
  id          Int    @id @default(autoincrement())
  amount      BigInt
  description String
  contractId  Int

  contract    Contract       @relation(fields: [contractId], references: [id])
  triggers    BonusTrigger[]
  triggeredAt DateTime?
  executedAt  DateTime?
}

model BonusTrigger {
  id              Int              @id @default(autoincrement())
  metric          BonusMetric
  threshold       Float
  period          BonusPeriod
  competitionType CompetitionType?
  teamScope       BonusTeamScope   @default(ALL)
  bonusId         Int

  bonus PerformanceBonus @relation(fields: [bonusId], references: [id])
}

model Transfer {
  id        Int          @id @default(autoincrement())
  type      TransferType
  date      DateTime
  startDate DateTime?
  endDate   DateTime?
  fee       Int?
  fromClub  String?
  toClub    String?
  playerId  String

  player Player  @relation(fields: [playerId], references: [id])
  recall Recall?
}

model Recall {
  id            Int          @id @default(autoincrement())
  status        RecallStatus @default(PENDING)
  requestedAt   DateTime     @default(now())
  approvedAt    DateTime?
  transferId    Int          @unique
  requestedById Int
  approvedById  Int?

  transfer    Transfer @relation(fields: [transferId], references: [id])
  requestedBy User     @relation("RecallRequestedBy", fields: [requestedById], references: [id])
  approvedBy  User?    @relation("RecallApprovedBy", fields: [approvedById], references: [id])
}

model TransferRequest {
  id           Int                   @id @default(autoincrement())
  status       TransferRequestStatus @default(DRAFT)
  type         TransferType
  playerId     String
  agencyId     Int
  fromClub     String?
  toClub       String?
  fee          Int?
  startDate    DateTime?
  endDate      DateTime?
  rejectReason String?

  requestedById Int
  reviewedById  Int?
  confirmedById Int?
  rejectedById  Int?

  reviewedAt  DateTime?
  confirmedAt DateTime?
  rejectedAt  DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  player      Player @relation(fields: [playerId], references: [id])
  agency      Agency @relation(fields: [agencyId], references: [id])
  requestedBy User   @relation("TRRequestedBy", fields: [requestedById], references: [id])
  reviewedBy  User?  @relation("TRReviewedBy", fields: [reviewedById], references: [id])
  confirmedBy User?  @relation("TRConfirmedBy", fields: [confirmedById], references: [id])
  rejectedBy  User?  @relation("TRRejectedBy", fields: [rejectedById], references: [id])
}

model Partner {
  id         Int          @id @default(autoincrement())
  type       PartnerType
  name       String
  country    String?
  website    String?
  address    String?
  phone      String?
  createdAt  DateTime     @default(now())
  tier       PartnerTier?
  tierReason String?

  injuries            Injury[]
  equipmentItems      EquipmentItem[]
  contracts           PartnerContract[]
  maintenanceRequests MaintenanceRequest[] @relation("MaintenancePartner")
  preventiveSchedules PreventiveSchedule[]
  contactLogs         PartnerContactLog[]
}

model PartnerContract {
  id             Int                   @id @default(autoincrement())
  partnerId      Int
  status         PartnerContractStatus @default(ACTIVE)
  startDate      DateTime
  endDate        DateTime
  sponsorshipFee Float?
  discountRate   Float?
  notes          String?
  responseHours  Int?
  resolutionDays Int?
  penaltyPerDay  Decimal?              @db.Decimal(12, 2)
  createdAt      DateTime              @default(now())

  partner      Partner       @relation(fields: [partnerId], references: [id])
  sponsorships Sponsorship[]
}

model EquipmentLoan {
  id              Int                 @id @default(autoincrement())
  status          EquipmentLoanStatus @default(REQUESTED)
  requestedAt     DateTime            @default(now())
  requestedById   Int
  approvedById    Int?
  issuedAt        DateTime?
  returnedAt      DateTime?
  returnNote      String?
  notes           String?
  equipmentItemId Int
  equipmentUnitId Int?

  requestedBy   User           @relation("LoanRequestedBy", fields: [requestedById], references: [id])
  approvedBy    User?          @relation("LoanApprovedBy", fields: [approvedById], references: [id])
  equipmentItem EquipmentItem  @relation(fields: [equipmentItemId], references: [id])
  equipmentUnit EquipmentUnit? @relation(fields: [equipmentUnitId], references: [id])
}

model Injury {
  id                 Int           @id @default(autoincrement())
  bodyPart           BodyPart
  cause              InjuryCause
  expectedReturnDate DateTime?
  status             InjuryStatus  @default(OCCURRED)
  occurredAt         DateTime      @default(now())
  priorWeeklyLoad    Int?
  playerId           String
  medicalStaffId     Int
  hospitalType       HospitalType?
  partnerId          Int?
  customHospitalName String?

  player                 Player            @relation(fields: [playerId], references: [id])
  medicalStaff           User              @relation(fields: [medicalStaffId], references: [id])
  partner                Partner?          @relation(fields: [partnerId], references: [id])
  medicalExpenses        MedicalExpense[]
  injuryReport           InjuryReport?
  assessment             InjuryAssessment?
  externalReports        ExternalReport[]
  incidentReports        IncidentReport[]
  dataRetentionFlaggedAt DateTime?
  dataRetentionReason    String?
  retainUntil            DateTime?
}

model InjuryAssessment {
  id                Int      @id @default(autoincrement())
  injuryId          Int      @unique
  painLevel         Int
  hasSwelling       Boolean  @default(false)
  romScore          Int
  strengthScore     Int
  sprintScore       Int
  jumpScore         Int
  psychScore        Int
  positionRiskScore Int
  medicalScore      Float
  functionalScore   Float
  modifierScore     Float
  totalScore        Float
  assessedAt        DateTime @default(now())
  assessedById      Int

  injury     Injury @relation(fields: [injuryId], references: [id], onDelete: Cascade)
  assessedBy User   @relation(fields: [assessedById], references: [id])
}

enum ExternalReportTarget {
  EDUCATION_OFFICE
  SCHOOL_SAFETY
  LEAGUE
  FEDERATION
  INSURANCE
  POLICE
  CHILD_PROTECTION_AGENCY
  FOOTBALL_ASSOCIATION
}

enum SafeguardReportStatus {
  RECEIVED
  UNDER_REVIEW
  RESOLVED
}

enum ExternalReportStatus {
  PENDING_SUBMISSION
  SUBMITTED
  SUPPLEMENT_REQUESTED
  COMPLETED
}

model ExternalReport {
  id                Int                  @id @default(autoincrement())
  injuryId          Int?
  incidentReportId  Int?
  safeguardReportId Int?
  target            ExternalReportTarget
  status            ExternalReportStatus @default(PENDING_SUBMISSION)
  reportData        Json
  dueDate           DateTime?
  submittedAt       DateTime?
  submittedNote     String?
  createdAt         DateTime             @default(now())
  updatedAt         DateTime             @updatedAt

  injury         Injury?         @relation(fields: [injuryId], references: [id], onDelete: Cascade)
  incidentReport IncidentReport? @relation(fields: [incidentReportId], references: [id], onDelete: Cascade)

  @@unique([injuryId, target])
  @@unique([incidentReportId, target])
}

model Match {
  id                 Int             @id @default(autoincrement())
  date               DateTime
  homeTeamName       String
  awayTeamName       String
  homeScore          Int?
  awayScore          Int?
  competitionType    CompetitionType
  venue              Venue?
  externalId         String?         @unique
  seasonId           Int
  teamId             Int?
  actualAttendance   Int?
  capacity           Int?
  priceRegular       Int?
  priceVip           Int?
  statSheetRaw       Json?
  statSheetImagePath String?

  season             Season              @relation(fields: [seasonId], references: [id])
  team               Team?               @relation(fields: [teamId], references: [id])
  playerMatchStats   PlayerMatchStats[]
  teamMatchStats     TeamMatchStats?
  tacticalAnalyses   TacticalAnalysis[]
  formationSnapshots FormationSnapshot[]
  squadPlayers       MatchSquad[]
  shotEvents         ShotEvent[]
  matchLineup        MatchLineup?
  incidentReports    IncidentReport[]
  salesRecords       SalesRecord[]
  fanPurchases       FanPurchase[]
  seatZones          SeatZone[]
}

model PlayerMatchStats {
  id                     Int      @id @default(autoincrement())
  goals                  Int?
  assists                Int?
  xG                     Float?
  xA                     Float?
  shots                  Int?
  keyPasses              Int?
  tackles                Int?
  tacklesAttempted       Int?
  tackleSuccessRate      Float?
  clearances             Int?
  interceptions          Int?
  saves                  Int?
  cleanSheet             Boolean?
  minutesPlayed          Int?
  passesAttempted        Int?
  passesCompleted        Int?
  aerialDuels            Int?
  aerialDuelsAttempted   Int?
  aerialDuelSuccessRate  Float?
  groundDuels            Int?
  groundDuelsAttempted   Int?
  groundDuelSuccessRate  Float?
  ballRecoveries         Int?
  turnovers              Int?
  distanceCovered        Float?
  sprint                 Float?
  clearCutChanceRate     Float?
  penaltyConversionRate  Float?
  freeKickConversionRate Float?
  foulsCommitted         Int?
  crossesCompleted       Int?
  shotsOnTarget          Int?
  shotsAllowed           Int?
  shotBlocked            Int?
  dribblesAttempted      Int?
  dribblesCompleted      Int?
  dribblesFailed         Int?
  longPassesAttempted    Int?
  longPassesCompleted    Int?
  matchId                Int
  playerId               String

  match  Match  @relation(fields: [matchId], references: [id])
  player Player @relation(fields: [playerId], references: [id])

  @@unique([matchId, playerId])
}

enum ShotResult {
  GOAL
  ON_TARGET
  OFF_TARGET
  BLOCKED
}

model ShotEvent {
  id                       Int        @id @default(autoincrement())
  matchId                  Int
  shooterId                String
  assisterId               String?
  assisterPositionOverride String?
  xG                       Float
  result                   ShotResult
  minute                   Int?

  match    Match   @relation(fields: [matchId], references: [id], onDelete: Cascade)
  shooter  Player  @relation("ShotShooter", fields: [shooterId], references: [id])
  assister Player? @relation("ShotAssister", fields: [assisterId], references: [id])

  @@index([matchId])
}

model TeamMatchStats {
  id               Int    @id @default(autoincrement())
  possession       Int
  shots            Int
  shotsOnTarget    Int
  passes           Int
  passAccuracy     Float
  fouls            Int
  yellowCards      Int
  redCards         Int
  xG               Float
  corners          Int
  offsides         Int
  tackles          Int
  interceptions    Int
  clearances       Int
  oppShots         Int?
  oppShotsOnTarget Int?
  oppCorners       Int?
  oppFouls         Int?
  oppYellowCards   Int?
  oppRedCards      Int?
  oppXG            Float?
  oppOffsides      Int?
  matchId          Int    @unique

  match Match @relation(fields: [matchId], references: [id])
}

model TrainingSession {
  id           Int         @id @default(autoincrement())
  date         DateTime
  goal         String
  sessionType  SessionType
  isApproved   Boolean     @default(false)
  cancelledAt  DateTime?
  createdAt    DateTime    @default(now())
  seasonId     Int
  createdById  Int
  approvedById Int?
  teamId       Int?
  targetLoad   Int?

  season          Season                @relation(fields: [seasonId], references: [id])
  team            Team?                 @relation(fields: [teamId], references: [id])
  createdBy       User                  @relation("TrainingSessionCreatedBy", fields: [createdById], references: [id])
  approvedBy      User?                 @relation("TrainingSessionApprovedBy", fields: [approvedById], references: [id])
  contents        TrainingContent[]
  participants    TrainingParticipant[]
  results         TrainingResult[]
  trainingLoads   TrainingLoad[]
  incidentReports IncidentReport[]
  playerBadges    PlayerBadge[]
}

model TrainingContent {
  id          Int          @id @default(autoincrement())
  phase       ContentPhase
  description String
  sessionId   Int

  session TrainingSession @relation(fields: [sessionId], references: [id])
}

model TrainingParticipant {
  sessionId Int
  playerId  String

  session TrainingSession @relation(fields: [sessionId], references: [id])
  player  Player          @relation(fields: [playerId], references: [id])

  @@id([sessionId, playerId])
}

model TrainingResult {
  id               Int              @id @default(autoincrement())
  attendance       AttendanceStatus
  feedback         String?
  performanceScore Int?
  scoredById       Int?
  sessionId        Int
  playerId         String

  session  TrainingSession    @relation(fields: [sessionId], references: [id])
  player   Player             @relation(fields: [playerId], references: [id])
  scoredBy User?              @relation("TrainingResultScorer", fields: [scoredById], references: [id])
  appeals  AttendanceAppeal[]

  @@unique([sessionId, playerId])
}

model TacticalAnalysis {
  id               Int            @id @default(autoincrement())
  phase            TacticalPhase
  formation        String?
  opponentAnalysis String?
  status           TacticalStatus @default(DRAFT)
  createdAt        DateTime       @default(now())
  matchId          Int
  seasonId         Int
  createdById      Int

  // PRE_MATCH
  opponentFormation     String?
  opponentKeyThreat     String? @db.Text
  opponentWeakness      String? @db.Text
  opponentKeyPlayer     String?
  opponentPressureScore Int?
  opponentSetPieceScore Int?
  opponentCounterScore  Int?
  // POST_MATCH
  tacticalCompliance    String? @db.Text
  concededAnalysis      String? @db.Text
  momPlayerId           String?
  momNote               String?
  improvementPlayerId   String?
  improvementNote       String?

  match             Match            @relation(fields: [matchId], references: [id])
  season            Season           @relation(fields: [seasonId], references: [id])
  createdBy         User             @relation(fields: [createdById], references: [id])
  lineup            TacticalLineup[]
  media             TacticalMedia[]
  momPlayer         Player?          @relation("MomPlayer", fields: [momPlayerId], references: [id])
  improvementPlayer Player?          @relation("ImprovementPlayer", fields: [improvementPlayerId], references: [id])
}

model TacticalLineup {
  id                 Int      @id @default(autoincrement())
  position           Position
  tacticalAnalysisId Int
  playerId           String

  tacticalAnalysis TacticalAnalysis @relation(fields: [tacticalAnalysisId], references: [id])
  player           Player           @relation(fields: [playerId], references: [id])
}

model TacticalMedia {
  id                 Int    @id @default(autoincrement())
  url                String
  type               String
  tacticalAnalysisId Int

  tacticalAnalysis TacticalAnalysis @relation(fields: [tacticalAnalysisId], references: [id])
}

model FormationSnapshot {
  id           Int      @id @default(autoincrement())
  matchId      Int
  minute       Int?
  formation    String
  changeReason String?
  createdById  Int
  createdAt    DateTime @default(now())

  match     Match @relation(fields: [matchId], references: [id], onDelete: Cascade)
  createdBy User  @relation("FormationSnapshotCreatedBy", fields: [createdById], references: [id])
}

model Notification {
  id        Int              @id @default(autoincrement())
  type      NotificationType
  title     String
  body      String
  entityId  Int?
  readAt    DateTime?
  createdAt DateTime         @default(now())
  userId    Int

  user User @relation(fields: [userId], references: [id])
}

model EquipmentItem {
  id                  Int               @id @default(autoincrement())
  name                String
  category            EquipmentCategory
  trackedIndividually Boolean
  quantity            Int?
  lowStockThreshold   Int?
  partnerId           Int?

  units         EquipmentUnit[]
  assignments   EquipmentAssignment[]
  partner       Partner?              @relation(fields: [partnerId], references: [id])
  loans         EquipmentLoan[]
  assetRequests AssetRequest[]
}

model EquipmentUnit {
  id                     Int                 @id @default(autoincrement())
  status                 EquipmentUnitStatus @default(AVAILABLE)
  equipmentItemId        Int
  serialNumber           String?
  purchasedAt            DateTime?
  purchaseValue          Decimal?            @db.Decimal(12, 2)
  bookValue              Decimal?            @db.Decimal(12, 2)
  depreciationRate       Decimal?            @db.Decimal(5, 4)
  depreciationMethod     DepreciationMethod?
  isHighValue            Boolean             @default(false)
  expiresAt              DateTime?
  lastInspectedAt        DateTime?
  inspectionIntervalDays Int?
  nextInspectionDue      DateTime?
  lastSanitizedAt        DateTime?
  sanitationStatus       String?
  disposedById           Int?
  disposedAt             DateTime?
  disposalNote           String?             @db.Text

  item                 EquipmentItem                  @relation(fields: [equipmentItemId], references: [id])
  assignments          EquipmentAssignment[]
  loans                EquipmentLoan[]
  disposedBy           User?                          @relation("EquipmentDisposer", fields: [disposedById], references: [id])
  disposalVerification EquipmentDisposalVerification?
}

model EquipmentAssignment {
  id              Int       @id @default(autoincrement())
  issuedAt        DateTime  @default(now())
  returnedAt      DateTime?
  playerId        String
  equipmentItemId Int?
  equipmentUnitId Int?

  player        Player         @relation(fields: [playerId], references: [id])
  equipmentItem EquipmentItem? @relation(fields: [equipmentItemId], references: [id])
  equipmentUnit EquipmentUnit? @relation(fields: [equipmentUnitId], references: [id])
}

model Prospect {
  id                Int              @id @default(autoincrement())
  name              String
  nationality       String?
  position          Position?
  currentTeam       String?
  notes             String?
  status            ProspectStatus   @default(ACTIVE)
  convertedPlayerId String?
  createdAt         DateTime         @default(now())
  createdById       Int?
  visaRequired      Boolean          @default(false)
  visaEligibility   VisaEligibility?

  convertedPlayer Player? @relation("ProspectConvertedPlayer", fields: [convertedPlayerId], references: [id])
  createdBy       User?   @relation("ProspectCreatedBy", fields: [createdById], references: [id])
}

model AuditLog {
  id        Int      @id @default(autoincrement())
  actorId   Int
  action    String
  targetId  String?
  detail    String?
  createdAt DateTime @default(now())

  actor User @relation(fields: [actorId], references: [id])
}

enum ReportType {
  PERFORMANCE
  MEDICAL
  TRAINING
  HR
  FINANCIAL
  ASSET
}

enum ReportStatus {
  DRAFT
  SUBMITTED
  REVIEWING
  FIRST_APPROVED
  SECOND_APPROVED
  APPROVED
  REJECTED
}

model Report {
  id              Int          @id @default(autoincrement())
  type            ReportType
  status          ReportStatus @default(DRAFT)
  title           String
  content         String       @db.Text
  fileUrl         String?
  fileName        String?
  rejectionReason String?

  authorId     Int
  author       User        @relation("ReportAuthor", fields: [authorId], references: [id])
  reviewerId   Int?
  reviewer     User?       @relation("ReportReviewer", fields: [reviewerId], references: [id])
  departmentId Int?
  department   Department? @relation(fields: [departmentId], references: [id], onDelete: SetNull)

  firstReviewerId  Int?
  firstReviewer    User?     @relation("ReportFirstReviewer", fields: [firstReviewerId], references: [id])
  firstReviewedAt  DateTime?
  secondReviewerId Int?
  secondReviewer   User?     @relation("ReportSecondReviewer", fields: [secondReviewerId], references: [id])
  secondReviewedAt DateTime?

  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt
  submittedAt DateTime?
  reviewedAt  DateTime?
  reviews     ReportReview[]
}

enum ExpenseCostCategory {
  OUTPATIENT
  EXAMINATION
  SURGERY
  REHABILITATION
  MEDICATION
}

enum ExpensePayerType {
  CLUB
  ASSOCIATION
  INDIVIDUAL
}

enum MedicalExpenseStatus {
  DRAFT
  SUBMITTED
  LEADER_APPROVED
  APPROVED
  REJECTED
}

model MedicalExpense {
  id              Int                  @id @default(autoincrement())
  status          MedicalExpenseStatus @default(DRAFT)
  injuryId        Int?
  playerId        String?
  receiptDate     DateTime
  costCategory    ExpenseCostCategory
  totalAmount     Int
  payerType       ExpensePayerType
  description     String?
  fileUrl         String?
  fileName        String?
  rejectionReason String?

  submittedById    Int
  leaderReviewerId Int?
  adminReviewerId  Int?

  submittedAt      DateTime?
  leaderReviewedAt DateTime?
  adminReviewedAt  DateTime?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  injury         Injury? @relation(fields: [injuryId], references: [id])
  player         Player? @relation(fields: [playerId], references: [id])
  submittedBy    User    @relation("MedicalExpenseSubmitter", fields: [submittedById], references: [id])
  leaderReviewer User?   @relation("MedicalExpenseLeaderReviewer", fields: [leaderReviewerId], references: [id])
  adminReviewer  User?   @relation("MedicalExpenseAdminReviewer", fields: [adminReviewerId], references: [id])
}

enum RehabStage {
  INITIAL_TREATMENT
  ACUTE_TREATMENT
  REHABILITATION
  RETURN_TRAINING
  CLEARED
}

enum RiskLevel {
  LOW
  MEDIUM
  HIGH
}

enum SecurityLevel {
  INTERNAL
  MEDICAL
  PRIVATE
}

model InjuryReport {
  id                  Int           @id @default(autoincrement())
  injuryId            Int           @unique
  diagnosisName       String?
  treatmentContent    String?       @db.Text
  rehabStage          RehabStage?
  trainingReturnDate  DateTime?
  matchAvailable      Boolean?
  reinjuryRisk        RiskLevel?
  medicalOpinion      String?       @db.Text
  securityLevel       SecurityLevel @default(INTERNAL)
  allowedActivities   String?       @db.Text
  rehabLoadPercentage Int?

  // 복귀 계획 3자 서명
  coachSignedAt     DateTime?
  coachSignedById   Int?
  trainerSignedAt   DateTime?
  trainerSignedById Int?
  medicalSignedAt   DateTime?
  medicalSignedById Int?

  createdById Int
  updatedById Int?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  injury        Injury @relation(fields: [injuryId], references: [id], onDelete: Cascade)
  createdBy     User   @relation("InjuryReportCreator", fields: [createdById], references: [id])
  updatedBy     User?  @relation("InjuryReportUpdater", fields: [updatedById], references: [id])
  coachSigner   User?  @relation("InjuryReportCoachSigner", fields: [coachSignedById], references: [id])
  trainerSigner User?  @relation("InjuryReportTrainerSigner", fields: [trainerSignedById], references: [id])
  medicalSigner User?  @relation("InjuryReportMedicalSigner", fields: [medicalSignedById], references: [id])
}

model CoachHiringRound {
  id                Int               @id @default(autoincrement())
  targetRole        CoachingRole
  fitScoreThreshold Int               @default(70)
  status            HiringRoundStatus @default(OPEN)
  deadline          DateTime?
  budget            Int?
  notes             String?
  result            String?
  createdById       Int
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt

  createdBy User    @relation("HiringRoundCreatedBy", fields: [createdById], references: [id])
  coaches   Coach[]
}

model Coach {
  id              Int              @id @default(autoincrement())
  name            String
  nationality     String?
  coachingRole    CoachingRole
  status          CoachStatus      @default(CANDIDATE)
  shortlistSource ShortlistSource?
  notes           String?
  isDeleted       Boolean          @default(false)
  packageLeadId   Int?
  hiringRoundId   Int?
  userId          Int?             @unique
  teamId          Int?
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt

  packageLead         Coach?                     @relation("CoachPackage", fields: [packageLeadId], references: [id])
  packageMembers      Coach[]                    @relation("CoachPackage")
  hiringRound         CoachHiringRound?          @relation(fields: [hiringRoundId], references: [id])
  user                User?                      @relation("CoachUser", fields: [userId], references: [id])
  team                Team?                      @relation(fields: [teamId], references: [id])
  tutorAssignments    CoachTutorAssignment[]
  headCoachEval       HeadCoachEvaluation?
  defensiveCoachEval  DefensiveCoachEvaluation?
  attackingCoachEval  AttackingCoachEvaluation?
  goalkeeperCoachEval GoalkeeperCoachEvaluation?
  tier2Eval           CoachTier2Evaluation?
  certifications      Certification[]            @relation("CoachCertifications")
}

model HeadCoachEvaluation {
  id                      Int       @id @default(autoincrement())
  coachId                 Int       @unique
  possession              Float?
  pressingIntensity       Float?
  progressivePassAccuracy Float?
  teamActivity            Float?
  philosophyFitScore      Float?
  dataSource              String?
  evaluatedAt             DateTime?

  coach Coach @relation(fields: [coachId], references: [id])
}

model DefensiveCoachEvaluation {
  id                Int       @id @default(autoincrement())
  coachId           Int       @unique
  tackleSuccessRate Float?
  clearances        Float?
  blocks            Float?
  defensiveErrors   Float?
  ballRecovery      Float?
  pressingIntensity Float?
  dataSource        String?
  evaluatedAt       DateTime?

  coach Coach @relation(fields: [coachId], references: [id])
}

model AttackingCoachEvaluation {
  id                      Int       @id @default(autoincrement())
  coachId                 Int       @unique
  xG                      Float?
  xA                      Float?
  chanceCreation          Float?
  dribbleSuccessRate      Float?
  progressivePassAccuracy Float?
  shotConversionRate      Float?
  goalInvolvement         Float?
  dataSource              String?
  evaluatedAt             DateTime?

  coach Coach @relation(fields: [coachId], references: [id])
}

model GoalkeeperCoachEvaluation {
  id                  Int       @id @default(autoincrement())
  coachId             Int       @unique
  psxG                Float?
  xGConcededDiff      Float?
  buildupPassAccuracy Float?
  dataSource          String?
  evaluatedAt         DateTime?

  coach Coach @relation(fields: [coachId], references: [id])
}

model CoachTier2Evaluation {
  id          Int       @id @default(autoincrement())
  coachId     Int       @unique
  fitScore    Int?
  notes       String?
  evaluatedAt DateTime?

  coach Coach @relation(fields: [coachId], references: [id])
}

model PlayerSecondaryPosition {
  id            Int      @id @default(autoincrement())
  playerId      String
  position      Position
  fitnessTarget Float
  createdAt     DateTime @default(now())

  player Player @relation(fields: [playerId], references: [id])

  @@unique([playerId, position])
}

model CoachingStaffEvaluation {
  id          Int      @id @default(autoincrement())
  staffUserId Int
  evaluatorId Int
  score       Int
  comment     String?
  evaluatedAt DateTime @default(now())

  staffUser User @relation("EvaluationReceiver", fields: [staffUserId], references: [id])
  evaluator User @relation("EvaluationGiver", fields: [evaluatorId], references: [id])
}

model CoachTutorAssignment {
  id                         Int                  @id @default(autoincrement())
  coachId                    Int
  type                       TutorType
  internalTutorId            Int?
  externalName               String?
  externalContact            String?
  sessionCount               Int                  @default(0)
  languageProficiency        LanguageProficiency?
  tacticalImplementationRate Float?
  createdAt                  DateTime             @default(now())
  updatedAt                  DateTime             @updatedAt

  coach         Coach @relation(fields: [coachId], references: [id])
  internalTutor User? @relation("TutorAssignments", fields: [internalTutorId], references: [id])
}

model CoachAvailability {
  id          Int      @id @default(autoincrement())
  userId      Int
  startDate   DateTime
  endDate     DateTime
  reason      String?
  createdById Int
  createdAt   DateTime @default(now())

  user      User @relation("CoachAvailabilityUser", fields: [userId], references: [id])
  createdBy User @relation("CoachAvailabilityCreatedBy", fields: [createdById], references: [id])
}

enum LoadUnit {
  KG
  MINUTES
  DISTANCE_M
  SETS
}

model TrainingLoad {
  id           Int       @id @default(autoincrement())
  playerId     String
  sessionId    Int
  rpe          Int
  load         Int?
  loadUnit     LoadUnit?
  version      Int       @default(1)
  previousRpe  Int?
  previousLoad Int?

  player  Player          @relation(fields: [playerId], references: [id])
  session TrainingSession @relation(fields: [sessionId], references: [id])

  @@unique([playerId, sessionId])
}

model PlayerDevelopmentPlan {
  id         Int                         @id @default(autoincrement())
  playerId   String
  coachId    Int
  seasonId   Int
  goals      String                      @db.Text
  notes      String?                     @db.Text
  status     PlayerDevelopmentPlanStatus @default(DRAFT)
  reviewedAt DateTime?
  createdAt  DateTime                    @default(now())
  updatedAt  DateTime                    @updatedAt

  player            Player             @relation(fields: [playerId], references: [id])
  coach             User               @relation("PDPCoach", fields: [coachId], references: [id])
  season            Season             @relation(fields: [seasonId], references: [id])
  growthEvaluations GrowthEvaluation[]

  @@unique([playerId, seasonId])
}

model TrainingVideo {
  id           Int          @id @default(autoincrement())
  title        String
  url          String
  tags         String[]
  sessionType  SessionType?
  uploadedById Int
  createdAt    DateTime     @default(now())
  aiSummary    String?

  uploader    User              @relation("VideoUploader", fields: [uploadedById], references: [id])
  assignments VideoAssignment[]
}

model VideoAssignment {
  id           Int       @id @default(autoincrement())
  videoId      Int
  playerId     String
  assignedById Int
  dueDate      DateTime?
  progressRate Int       @default(0)
  note         String?
  createdAt    DateTime  @default(now())

  video      TrainingVideo @relation(fields: [videoId], references: [id])
  player     Player        @relation(fields: [playerId], references: [id])
  assignedBy User          @relation("VideoAssigner", fields: [assignedById], references: [id])

  @@unique([videoId, playerId])
}

model TrainingReference {
  id          Int             @id @default(autoincrement())
  sessionType SessionType
  title       String
  url         String
  source      ReferenceSource
  tags        String[]
  addedById   Int
  createdAt   DateTime        @default(now())

  addedBy User @relation(fields: [addedById], references: [id])
}

enum ReferenceSource {
  INTERNAL
  EXTERNAL
}

enum PlayerCallupStatus {
  REQUESTED
  DOCS_SUBMITTED
  APPROVED
  REJECTED
  COMPLETED
}

enum CallupType {
  TRAINING
  OFFICIAL
}

model YouthRegistration {
  id                    Int                     @id @default(autoincrement())
  playerName            String
  birthDate             DateTime
  preferredJerseyNumber Int?
  teamId                Int
  guardianId            Int?
  status                YouthRegistrationStatus @default(PENDING)
  requestedById         Int
  rejectionReason       String?
  createdAt             DateTime                @default(now())
  updatedAt             DateTime                @updatedAt

  team        Team  @relation(fields: [teamId], references: [id])
  guardian    User? @relation("GuardianRegistrations", fields: [guardianId], references: [id])
  requestedBy User  @relation("YouthRegistrationRequester", fields: [requestedById], references: [id])
}

model GuardianInviteCode {
  id         Int       @id @default(autoincrement())
  code       String    @unique
  playerId   String
  issuedById Int
  usedById   Int?
  expiresAt  DateTime
  usedAt     DateTime?
  createdAt  DateTime  @default(now())

  player   Player @relation(fields: [playerId], references: [id])
  issuedBy User   @relation("GuardianCodeIssuer", fields: [issuedById], references: [id])
  usedBy   User?  @relation("GuardianCodeUser", fields: [usedById], references: [id])
}

model IncidentReport {
  id               Int                  @id @default(autoincrement())
  playerId         String
  teamId           Int
  type             IncidentType
  matchId          Int?
  sessionId        Int?
  description      String               @db.Text
  reportedById     Int
  supervisorSigned Boolean              @default(false)
  medicalSigned    Boolean              @default(false)
  injuryId         Int?
  status           IncidentReportStatus @default(DRAFT)
  createdAt        DateTime             @default(now())
  updatedAt        DateTime             @updatedAt

  player          Player           @relation(fields: [playerId], references: [id])
  team            Team             @relation(fields: [teamId], references: [id])
  match           Match?           @relation(fields: [matchId], references: [id])
  session         TrainingSession? @relation(fields: [sessionId], references: [id])
  reportedBy      User             @relation("IncidentReporter", fields: [reportedById], references: [id])
  injury          Injury?          @relation(fields: [injuryId], references: [id])
  externalReports ExternalReport[]
}

model SafeguardReport {
  id            Int                   @id @default(autoincrement())
  description   String                @db.Text
  contactInfo   String?
  accusedUserId Int?
  status        SafeguardReportStatus @default(RECEIVED)
  resolvedNote  String?
  createdAt     DateTime              @default(now())
  updatedAt     DateTime              @updatedAt

  accusedUser User? @relation("SafeguardAccused", fields: [accusedUserId], references: [id])
}

model GrowthEvaluation {
  id                  Int       @id @default(autoincrement())
  playerId            String
  coachId             Int
  planId              Int?
  year                Int
  month               Int
  isPublished         Boolean   @default(false)
  publishedAt         DateTime?
  attitudeScore       Int
  attitudeComment     String    @db.Text
  fundamentalsScore   Int
  fundamentalsComment String    @db.Text
  spatialScore        Int
  spatialComment      String    @db.Text
  physicalScore       Int
  physicalComment     String    @db.Text
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt

  player Player                 @relation(fields: [playerId], references: [id])
  coach  User                   @relation("CoachEvaluations", fields: [coachId], references: [id])
  plan   PlayerDevelopmentPlan? @relation(fields: [planId], references: [id])

  @@unique([playerId, year, month])
}

model PlayerBadge {
  id        Int       @id @default(autoincrement())
  playerId  String
  coachId   Int
  sessionId Int?
  badgeType BadgeType
  awardedAt DateTime  @default(now())
  note      String?

  player  Player           @relation(fields: [playerId], references: [id])
  coach   User             @relation("CoachBadges", fields: [coachId], references: [id])
  session TrainingSession? @relation(fields: [sessionId], references: [id])
}

model PlayerCallup {
  id                  Int                @id @default(autoincrement())
  playerId            String
  fromTeamId          Int
  toTeamId            Int
  requestedById       Int
  approvedById        Int?
  reason              String
  rejectionReason     String?
  status              PlayerCallupStatus @default(REQUESTED)
  callupType          CallupType         @default(OFFICIAL)
  youthCoachConfirmed Boolean            @default(false)
  medicalConfirmed    Boolean            @default(false)
  requiredDocuments   String[]           @default([])
  startDate           DateTime
  endDate             DateTime?
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt

  player      Player @relation(fields: [playerId], references: [id])
  fromTeam    Team   @relation("CallupFromTeam", fields: [fromTeamId], references: [id])
  toTeam      Team   @relation("CallupToTeam", fields: [toTeamId], references: [id])
  requestedBy User   @relation("CallupRequestedBy", fields: [requestedById], references: [id])
  approvedBy  User?  @relation("CallupApprovedBy", fields: [approvedById], references: [id])
}

model LoginHistory {
  id        Int      @id @default(autoincrement())
  userId    Int?
  email     String
  ip        String
  userAgent String
  success   Boolean
  createdAt DateTime @default(now())

  user User? @relation(fields: [userId], references: [id])
}

model JerseyNumber {
  id       Int                @id @default(autoincrement())
  number   Int
  status   JerseyNumberStatus @default(AVAILABLE)
  teamId   Int
  playerId String?

  team   Team    @relation(fields: [teamId], references: [id])
  player Player? @relation(fields: [playerId], references: [id])

  @@unique([number, teamId])
  @@index([playerId])
}

model MarketValueHistory {
  id           Int               @id @default(autoincrement())
  value        Float
  source       MarketValueSource
  recordedAt   DateTime          @default(now())
  recordedById Int?
  playerId     String

  player     Player @relation(fields: [playerId], references: [id])
  recordedBy User?  @relation(fields: [recordedById], references: [id])

  @@index([playerId, recordedAt])
}

model MatchSquad {
  id            Int       @id @default(autoincrement())
  matchId       Int
  playerId      String
  isConfirmed   Boolean   @default(false)
  confirmedAt   DateTime?
  confirmedById Int?
  notifiedAt    DateTime?

  match       Match  @relation(fields: [matchId], references: [id])
  player      Player @relation(fields: [playerId], references: [id])
  confirmedBy User?  @relation("SquadConfirmations", fields: [confirmedById], references: [id])

  @@unique([matchId, playerId])
  @@index([matchId])
}

model MatchLineup {
  id            Int       @id @default(autoincrement())
  matchId       Int       @unique
  formation     String
  isConfirmed   Boolean   @default(false)
  confirmedAt   DateTime?
  confirmedById Int?

  match       Match        @relation(fields: [matchId], references: [id])
  confirmedBy User?        @relation("LineupConfirmations", fields: [confirmedById], references: [id])
  slots       LineupSlot[]

  @@index([matchId])
}

model LineupSlot {
  id        Int     @id @default(autoincrement())
  lineupId  Int
  playerId  String
  slotKey   String
  isStarter Boolean @default(true)

  lineup MatchLineup @relation(fields: [lineupId], references: [id], onDelete: Cascade)
  player Player      @relation(fields: [playerId], references: [id])

  @@unique([lineupId, slotKey])
  @@unique([lineupId, playerId])
  @@index([lineupId])
}

enum PaymentMethod {
  PG
  BANK_TRANSFER
}

enum FeeStatus {
  PENDING
  SUBMITTED
  FIRST_APPROVED
  PAID
  OVERDUE
  LOCKED
}

model AcademyFee {
  id                 Int            @id @default(autoincrement())
  playerId           String
  guardianId         Int
  amount             Int
  dueDate            DateTime
  status             FeeStatus      @default(PENDING)
  paidAt             DateTime?
  paymentProofUrl    String?
  paymentSubmittedAt DateTime?
  paymentMethod      PaymentMethod?
  pgTransactionId    String?
  receiptIssuedAt    DateTime?
  year               Int
  month              Int
  createdAt          DateTime       @default(now())
  updatedAt          DateTime       @updatedAt

  player   Player @relation(fields: [playerId], references: [id])
  guardian User   @relation("GuardianFees", fields: [guardianId], references: [id])

  @@unique([playerId, year, month])
}

model ClubSettings {
  id                       Int    @id @default(1)
  currency                 String @default("KRW")
  ibiBeta                  Float  @default(1.0)
  planApprovalLimit        Int    @default(10000000)
  maintenanceCostLimit     Int    @default(1000000)
  complimentaryTicketLimit Int    @default(10)
  reviewerDeptMap          Json?
}

model StaffRecord {
  id           Int         @id @default(autoincrement())
  name         String
  role         String
  departmentId Int?
  department   Department? @relation(fields: [departmentId], references: [id], onDelete: SetNull)
  phone        String?
  email        String?     @unique
  employeeId   String?     @unique
  isActive     Boolean     @default(true)
  terminatedAt DateTime?
  notes        String?
  createdById  Int
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  createdBy           User            @relation("StaffRecordCreator", fields: [createdById], references: [id])
  staffSalaries       StaffSalary[]
  employmentStartDate DateTime?
  employmentEndDate   DateTime?
  certifications      Certification[] @relation("StaffCertifications")
}

model FinancialReport {
  id                          Int                         @id @default(autoincrement())
  seasonId                    Int                         @unique
  totalRevenue                Float
  // Planned revenue breakdown (계획값). Actuals are always computed live from
  // source tables (SalesRecord, SponsorshipPayment, LedgerEntry). Do not write
  // actuals into these fields — that broke dashboards prior to 2026-08-22.
  plannedRevenueTicket        Float                       @default(0)
  plannedRevenueSponsorship   Float                       @default(0)
  plannedRevenueBroadcast     Float                       @default(0)
  plannedRevenueMerchandise   Float                       @default(0)
  plannedRevenueSubsidy       Float                       @default(0)
  plannedRevenueParentCompany Float                       @default(0)
  plannedRevenueAcademyFee    Float                       @default(0)
  plannedRevenueOther         Float                       @default(0)
  totalOperatingBudget        Int?
  contingencyReserve          Int?                        @default(0)
  playerSalaryBudget          Int?
  note                        String?
  // 전년도 이월금 (auto-computed from prev season's approved MonthlySettlementReport
  // net income at closeSeason time, or manually overridden via
  // PATCH /financial-reports/:seasonId/carryover).
  carryOverFromPrev           Float                       @default(0)
  carryOverOverriddenById     Int?
  carryOverOverriddenAt       DateTime?
  carryOverOverrideReason     String?
  createdAt                   DateTime                    @default(now())
  updatedAt                   DateTime                    @updatedAt
  season                      Season                      @relation(fields: [seasonId], references: [id], onDelete: Cascade)
  carryOverOverriddenBy       User?                       @relation("CarryOverOverrider", fields: [carryOverOverriddenById], references: [id])
  budgetCategoryPlans         BudgetCategoryPlan[]
  overrideLogs                BudgetOverrideLog[]
  revenueLogs                 FinancialReportRevenueLog[]
  revenueAdjustments          RevenueAdjustment[]         @relation("SeasonAdj")
}

model Department {
  id                Int                        @id @default(autoincrement())
  name              String
  clubId            Int?
  isActive          Boolean                    @default(true)
  createdAt         DateTime                   @default(now())
  updatedAt         DateTime                   @updatedAt
  category          DepartmentCategory?
  parentId          Int?
  headId            Int?
  club              Club?                      @relation(fields: [clubId], references: [id])
  parent            Department?                @relation("DepartmentHierarchy", fields: [parentId], references: [id], onDelete: SetNull)
  children          Department[]               @relation("DepartmentHierarchy")
  head              User?                      @relation("DepartmentHead", fields: [headId], references: [id], onDelete: SetNull)
  staffRecords      StaffRecord[]
  postings          JobPosting[]
  members           UserDepartment[]
  ibiConfigs        DepartmentIbiConfig[]
  planReports       PlanReport[]
  subjectConfigs    DepartmentReviewerConfig[] @relation("SubjectDept")
  reviewerConfigs   DepartmentReviewerConfig[] @relation("ReviewerDept")
  planReviews       PlanReview[]               @relation("PlanReviewerDept")
  reportReviews     ReportReview[]             @relation("ReportReviewerDept")
  reports           Report[]
  surveyTargets     SurveyTargetDept[]         @relation("SurveyTargetDept")
  surveyResponses   SurveyResponse[]           @relation("SurveyResponse")
  budgetLines       BudgetLine[]
  assetRequests     AssetRequest[]
  operatingExpenses OperatingExpense[]

  @@unique([name, clubId])
}

enum DeptRole {
  MANAGER
  MEMBER
}

model UserDepartment {
  userId       Int
  departmentId Int
  role         DeptRole   @default(MEMBER)
  joinedAt     DateTime   @default(now())
  user         User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  department   Department @relation(fields: [departmentId], references: [id], onDelete: Cascade)

  @@id([userId, departmentId])
}

// ─────────────────────────────────────────────
// Knapsack 운영비 예산 관리
// ─────────────────────────────────────────────

// ─────────────────────────────────────────────
// Expense Category (table-backed, replacing OperatingCategory enum)
// ─────────────────────────────────────────────

model ExpenseCategory {
  id        Int      @id @default(autoincrement())
  code      String   @unique
  label     String
  sortOrder Int      @default(0)
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  operatingExpenses   OperatingExpense[]
  budgetCategoryPlans BudgetCategoryPlan[]
  budgetLines         BudgetLine[]
  budgetOverrideLogs  BudgetOverrideLog[]
  assetRequests       AssetRequest[]
}

enum BudgetStatus {
  DRAFT
  SUBMITTED
  APPROVED
  LOCKED
}

enum AdjustmentType {
  CARRYOVER
  INCREASE
  DECREASE
  TRANSFER
}

enum AdjustmentStatus {
  PENDING
  APPROVED
  REJECTED
}

model BudgetCategoryPlan {
  id                Int             @id @default(autoincrement())
  financialReportId Int
  categoryId        Int
  mandatoryMinimum  Int             @default(0)
  knapsackAllocated Int?
  sortOrder         Int             @default(0)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt
  financialReport   FinancialReport @relation(fields: [financialReportId], references: [id], onDelete: Cascade)
  expenseCategory   ExpenseCategory @relation(fields: [categoryId], references: [id])
  tiers             BudgetTier[]

  @@unique([financialReportId, categoryId])
}

model BudgetTier {
  id             Int                @id @default(autoincrement())
  categoryPlanId Int
  name           String
  cost           Int
  value          Int
  isSelected     Boolean            @default(false)
  sortOrder      Int                @default(0)
  createdAt      DateTime           @default(now())
  updatedAt      DateTime           @updatedAt
  categoryPlan   BudgetCategoryPlan @relation(fields: [categoryPlanId], references: [id], onDelete: Cascade)
}

model BudgetOverrideLog {
  id                Int             @id @default(autoincrement())
  financialReportId Int
  categoryId        Int
  amount            Int
  reason            String
  createdById       Int
  createdAt         DateTime        @default(now())
  status            OverrideStatus  @default(PENDING)
  reviewedById      Int?
  reviewedAt        DateTime?
  reviewNote        String?
  financialReport   FinancialReport @relation(fields: [financialReportId], references: [id], onDelete: Cascade)
  createdBy         User            @relation("BudgetOverrideLogCreator", fields: [createdById], references: [id])
  reviewedBy        User?           @relation("BudgetOverrideLogReviewer", fields: [reviewedById], references: [id])
  expenseCategory   ExpenseCategory @relation(fields: [categoryId], references: [id])
}

enum ExpenseCostType {
  FIXED // 고정지출비 — 매월 정기 지출 (임대료, 구독료 등)
  VARIABLE // 변동비 — 발생 시점·금액이 다변화 (일회성 구매, 이동경비 등)
  CONTINGENCY // 예비비 — 예상 밖 긴급 지출 (사고, 시설 파손 등)
}

model OperatingExpense {
  id                 Int              @id @default(autoincrement())
  seasonId           Int
  categoryId         Int
  costType           ExpenseCostType?
  amount             Int
  date               DateTime
  note               String?
  createdById        Int
  paidAt             DateTime?
  paidById           Int?
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  deletedAt          DateTime?
  deletionReason     String?
  accountCodeId      Int?
  status             ExpenseStatus    @default(PENDING)
  budgetLineId       Int
  firstApprovedById  Int?
  firstApprovedAt    DateTime?
  approvedById       Int?
  approvedAt         DateTime?
  rejectedById       Int?
  rejectedAt         DateTime?
  rejectionReason    String?
  cancelledById      Int?
  cancelledAt        DateTime?
  cancellationReason String?
  departmentId       Int?
  season             Season           @relation(fields: [seasonId], references: [id])
  createdBy          User             @relation("OperatingExpenseCreator", fields: [createdById], references: [id])
  paidBy             User?            @relation("OperatingExpensePayer", fields: [paidById], references: [id])
  accountCode        AccountCode?     @relation(fields: [accountCodeId], references: [id])
  expenseCategory    ExpenseCategory  @relation(fields: [categoryId], references: [id])
  budgetLine         BudgetLine       @relation(fields: [budgetLineId], references: [id])
  firstApprovedBy    User?            @relation("OperatingExpenseFirstApprover", fields: [firstApprovedById], references: [id])
  approvedBy         User?            @relation("OperatingExpenseApprover", fields: [approvedById], references: [id])
  rejectedBy         User?            @relation("OperatingExpenseRejecter", fields: [rejectedById], references: [id])
  cancelledBy        User?            @relation("OperatingExpenseCanceller", fields: [cancelledById], references: [id])
  department         Department?      @relation(fields: [departmentId], references: [id])
  assetRequests      AssetRequest[]
}

// ─────────────────────────────────────────────
// 예산 통제
// ─────────────────────────────────────────────

model BudgetHeader {
  id           Int                @id @default(autoincrement())
  seasonId     Int
  version      Int                @default(1)
  status       BudgetStatus       @default(DRAFT)
  name         String
  totalBudget  Int                @default(0)
  note         String?
  createdById  Int
  approvedById Int?
  approvedAt   DateTime?
  createdAt    DateTime           @default(now())
  updatedAt    DateTime           @updatedAt
  season       Season             @relation(fields: [seasonId], references: [id])
  createdBy    User               @relation("BudgetHeaderCreator", fields: [createdById], references: [id])
  approvedBy   User?              @relation("BudgetHeaderApprover", fields: [approvedById], references: [id])
  lines        BudgetLine[]
  adjustments  BudgetAdjustment[]

  @@unique([seasonId, version])
}

model BudgetLine {
  id                Int                @id @default(autoincrement())
  budgetHeaderId    Int
  departmentId      Int?
  categoryId        Int
  year              Int
  month             Int?
  originalAmount    Int                @default(0)
  note              String?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  budgetHeader      BudgetHeader       @relation(fields: [budgetHeaderId], references: [id], onDelete: Cascade)
  department        Department?        @relation(fields: [departmentId], references: [id])
  expenseCategory   ExpenseCategory    @relation(fields: [categoryId], references: [id])
  operatingExpenses OperatingExpense[]
}

model BudgetAdjustment {
  id             Int              @id @default(autoincrement())
  budgetHeaderId Int
  type           AdjustmentType
  amount         Int
  fromLineId     Int?
  toLineId       Int?
  reason         String
  status         AdjustmentStatus @default(PENDING)
  approvedById   Int?
  approvedAt     DateTime?
  createdById    Int
  createdAt      DateTime         @default(now())
  budgetHeader   BudgetHeader     @relation(fields: [budgetHeaderId], references: [id], onDelete: Cascade)
  createdBy      User             @relation("AdjustmentCreator", fields: [createdById], references: [id])
  approvedBy     User?            @relation("AdjustmentApprover", fields: [approvedById], references: [id])
}

// ─────────────────────────────────────────────
// 채용 파이프라인
// ─────────────────────────────────────────────

enum JobPostingStatus {
  DRAFT
  OPEN
  CLOSED
}

enum JobApplicationStatus {
  APPLIED
  SCREENING
  INTERVIEW_1
  INTERVIEW_2
  REFERENCE_CHECK
  OFFERED
  ONBOARDED
  REJECTED
}

enum InterviewRound {
  ROUND_1
  ROUND_2
}

enum InterviewResult {
  PENDING
  PASS
  FAIL
}

enum ReferenceCheckResult {
  PENDING
  CLEAR
  FLAGGED
}

model JobPosting {
  id               Int              @id @default(autoincrement())
  title            String
  departmentId     Int?
  headcount        Int              @default(1)
  description      String
  status           JobPostingStatus @default(DRAFT)
  externalJobId    String?
  createdById      Int
  approvedById     Int?
  approvedAt       DateTime?
  closedAt         DateTime?
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt
  budget           Int?
  planReportId     Int?             @unique
  hiringPlanItemId Int?

  department     Department?      @relation(fields: [departmentId], references: [id])
  createdBy      User             @relation("JobPostingCreator", fields: [createdById], references: [id])
  approvedBy     User?            @relation("JobPostingApprover", fields: [approvedById], references: [id])
  applications   JobApplication[]
  planReport     PlanReport?      @relation(fields: [planReportId], references: [id])
  hiringPlanItem HiringPlanItem?  @relation(fields: [hiringPlanItemId], references: [id], onDelete: SetNull)
}

model JobApplication {
  id                  Int                   @id @default(autoincrement())
  postingId           Int
  applicantName       String
  email               String
  phone               String?
  resumeUrl           String?
  status              JobApplicationStatus  @default(APPLIED)
  rejectedAt          DateTime?
  rejectionReason     String?
  previousStatus      JobApplicationStatus?
  offeredAt           DateTime?
  offeredById         Int?
  createdAt           DateTime              @default(now())
  updatedAt           DateTime              @updatedAt
  source              ApplicationSource     @default(DIRECT)
  externalApplicantId String?

  posting                 JobPosting      @relation(fields: [postingId], references: [id])
  offeredBy               User?           @relation("ApplicationOffer", fields: [offeredById], references: [id])
  interviews              Interview[]
  referenceCheck          ReferenceCheck?
  onboarding              Onboarding?
  referenceCheckConsent   Boolean?
  referenceCheckConsentAt DateTime?
  dataRetentionDeadline   DateTime?
  dataRetentionFlaggedAt  DateTime?

  @@unique([postingId, externalApplicantId])
  @@unique([postingId, email])
}

model Interview {
  id             Int             @id @default(autoincrement())
  applicationId  Int
  round          InterviewRound
  scheduledAt    DateTime?
  interviewerIds Int[]
  scoreSkill     Int?
  scoreComm      Int?
  scoreCulture   Int?
  comment        String?
  result         InterviewResult @default(PENDING)
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt

  application       JobApplication     @relation(fields: [applicationId], references: [id])
  interviewerScores InterviewerScore[]

  @@unique([applicationId, round])
}

model InterviewerScore {
  id            Int     @id @default(autoincrement())
  interviewId   Int
  interviewerId Int
  scoreSkill    Int?
  scoreComm     Int?
  scoreCulture  Int?
  comment       String?

  interview   Interview @relation(fields: [interviewId], references: [id], onDelete: Cascade)
  interviewer User      @relation("InterviewerScores", fields: [interviewerId], references: [id])

  @@unique([interviewId, interviewerId])
}

model ReferenceCheck {
  id            Int                  @id @default(autoincrement())
  applicationId Int                  @unique
  contactName   String
  relationship  String
  result        ReferenceCheckResult @default(PENDING)
  notes         String?
  createdAt     DateTime             @default(now())
  updatedAt     DateTime             @updatedAt

  application JobApplication @relation(fields: [applicationId], references: [id])
}

model Onboarding {
  id              Int       @id @default(autoincrement())
  applicationId   Int       @unique
  userId          Int?      @unique
  otpCode         String
  otpExpiresAt    DateTime
  emailVerifiedAt DateTime?
  mfaRegisteredAt DateTime?
  completedAt     DateTime?
  createdAt       DateTime  @default(now())

  application JobApplication @relation(fields: [applicationId], references: [id])
  user        User?          @relation("OnboardingUser", fields: [userId], references: [id])
}

// ─────────────────────────────────────────────
// 채용 자동화
// ─────────────────────────────────────────────

model LeagueLevelWeightConfig {
  id          Int                @id @default(autoincrement())
  leagueLevel LeagueLevel
  category    DepartmentCategory
  weight      Decimal            @db.Decimal(5, 4)
  createdAt   DateTime           @default(now())
  updatedAt   DateTime           @updatedAt

  @@unique([leagueLevel, category])
}

model DepartmentIbiConfig {
  id              Int      @id @default(autoincrement())
  departmentId    Int
  jobTitle        String
  coreTaskRatio   Decimal  @db.Decimal(4, 3)
  replacementDays Int
  backupHeadcount Int
  effectiveFrom   DateTime
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  department Department @relation(fields: [departmentId], references: [id])

  @@unique([departmentId, jobTitle, effectiveFrom])
}

model SeasonComplianceCheck {
  id                  Int       @id @default(autoincrement())
  seasonId            Int       @unique
  afcQualificationMet Boolean   @default(false)
  officeStaffCountMet Boolean   @default(false)
  checkedAt           DateTime?
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt

  season Season @relation(fields: [seasonId], references: [id])
}

model ComplianceDeadline {
  id                Int      @id @default(autoincrement())
  name              String
  deadlineDate      DateTime
  triggerDaysBefore Int
  betaMultiplier    Decimal  @db.Decimal(4, 2)
  isActive          Boolean  @default(true)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
}

// ─────────────────────────────────────────────
// 시설 관리
// ─────────────────────────────────────────────

model FacilityInspection {
  id                Int              @id @default(autoincrement())
  type              InspectionType
  facilityZone      FacilityZone
  result            InspectionResult
  sanitationScore   Int?
  isStatutory       Boolean          @default(false)
  certificateUrl    String?
  statutoryDeadline DateTime?
  inspectedById     Int
  inspectedAt       DateTime         @default(now())
  notes             String?
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  inspectedBy         User                 @relation("FacilityInspector", fields: [inspectedById], references: [id])
  maintenanceRequests MaintenanceRequest[]
}

model MaintenanceRequest {
  id                 Int                 @id @default(autoincrement())
  title              String
  description        String              @db.Text
  priority           MaintenancePriority
  status             MaintenanceStatus   @default(OPEN)
  sourceInspectionId Int?
  postIncidentReport String?             @db.Text
  estimatedCost      Decimal?            @db.Decimal(12, 2)
  actualCost         Decimal?            @db.Decimal(12, 2)
  resolvedAt         DateTime?
  approvedById       Int?
  approvedAt         DateTime?
  gmApprovedById     Int?
  gmApprovedAt       DateTime?
  rejectionReason    String?
  isLocked           Boolean             @default(false)
  financeSubmittedAt DateTime?
  createdById        Int
  partnerId          Int?
  sourceScheduleId   Int?
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt

  sourceInspection FacilityInspection? @relation(fields: [sourceInspectionId], references: [id])
  createdBy        User                @relation("MaintenanceRequestCreator", fields: [createdById], references: [id])
  approvedBy       User?               @relation("MaintenanceFMApprover", fields: [approvedById], references: [id])
  gmApprovedBy     User?               @relation("MaintenanceGMApprover", fields: [gmApprovedById], references: [id])
  partner          Partner?            @relation("MaintenancePartner", fields: [partnerId], references: [id])
  sourceSchedule   PreventiveSchedule? @relation(fields: [sourceScheduleId], references: [id])
}

// ─────────────────────────────────────────────
// 인증 관리 (Certification)
// ─────────────────────────────────────────────

model Certification {
  id              Int               @id @default(autoincrement())
  certType        CertificationType
  entityType      CertEntityType
  issuingBody     String
  issuedAt        DateTime
  expiresAt       DateTime
  status          CertStatus        @default(DRAFT)
  isLocked        Boolean           @default(false)
  documentUrl     String?
  reminderDays    Int[]             @default([90, 60, 30])
  notes           String?
  rejectionReason String?

  ownerId        Int
  approvedById   Int?
  approvedAt     DateTime?
  gmApprovedById Int?
  gmApprovedAt   DateTime?

  playerId     String?
  coachId      Int?
  staffId      Int?
  facilityZone FacilityZone?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  owner        User                       @relation("CertOwner", fields: [ownerId], references: [id])
  approvedBy   User?                      @relation("CertFirstApprover", fields: [approvedById], references: [id])
  gmApprovedBy User?                      @relation("CertSecondApprover", fields: [gmApprovedById], references: [id])
  player       Player?                    @relation("PlayerCertifications", fields: [playerId], references: [id])
  coach        Coach?                     @relation("CoachCertifications", fields: [coachId], references: [id])
  staff        StaffRecord?               @relation("StaffCertifications", fields: [staffId], references: [id])
  reminders    CertificationReminderLog[]

  @@index([expiresAt])
  @@index([status])
  @@index([entityType])
  @@index([playerId])
  @@index([coachId])
  @@index([staffId])
}

model CertificationReminderLog {
  id              Int      @id @default(autoincrement())
  certificationId Int
  daysThreshold   Int
  sentAt          DateTime @default(now())

  certification Certification @relation(fields: [certificationId], references: [id], onDelete: Cascade)

  @@unique([certificationId, daysThreshold])
}

// ─────────────────────────────────────────────
// 스폰서십
// ─────────────────────────────────────────────

model Sponsorship {
  id                  Int             @id @default(autoincrement())
  sponsorName         String
  type                SponsorType
  totalFee            Decimal         @db.Decimal(14, 2)
  contractStart       DateTime
  contractEnd         DateTime
  paymentSchedule     PaymentSchedule
  attachedContractId  Int?
  createdById         Int
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt
  exposureCount       Int?
  mediaValue          Decimal?        @db.Decimal(15, 2)
  fanReach            Int?
  currency            CurrencyCode    @default(KRW)
  targetExposureCount Int?
  targetFanReach      Int?
  targetMediaValue    Decimal?        @db.Decimal(15, 2)

  // 국내 계좌
  domesticBankName      String?
  domesticAccountNumber String?
  domesticAccountHolder String?
  // 영국 계좌
  ukBankName            String?
  ukSortCode            String?
  ukAccountNumber       String?
  ukSwiftBic            String?

  // 국내/해외 구분
  isOverseas        Boolean @default(false)
  // 국내 전용
  businessRegNumber String?
  postalCode        String?
  address           String?
  addressDetail     String?
  // 해외 전용
  taxId             String?
  overseasAddress   String?

  attachedContract PartnerContract?           @relation(fields: [attachedContractId], references: [id])
  createdBy        User                       @relation("SponsorshipCreator", fields: [createdById], references: [id])
  payments         SponsorshipPayment[]
  clauses          SponsorshipClause[]
  exposureEvents   SponsorshipExposureEvent[]
  deletedAt        DateTime?
}

model SponsorshipPayment {
  id               Int                      @id @default(autoincrement())
  sponsorshipId    Int
  dueDate          DateTime
  amount           Decimal                  @db.Decimal(14, 2)
  paidAt           DateTime?
  status           SponsorshipPaymentStatus @default(PENDING)
  createdAt        DateTime                 @default(now())
  updatedAt        DateTime                 @updatedAt
  adjustedAmount   Decimal?                 @db.Decimal(14, 2)
  adjustmentReason String?
  appliedClauseId  Int?

  sponsorship   Sponsorship        @relation(fields: [sponsorshipId], references: [id], onDelete: Cascade)
  appliedClause SponsorshipClause? @relation(fields: [appliedClauseId], references: [id], onDelete: SetNull)
}

// ─────────────────────────────────────────────
// 급여 관리
// ─────────────────────────────────────────────

model PayrollConfig {
  id            Int            @id @default(autoincrement())
  country       PayrollCountry
  insuranceType String
  employeeRate  Decimal        @db.Decimal(6, 5)
  employerRate  Decimal        @db.Decimal(6, 5)
  effectiveFrom DateTime
  createdAt     DateTime       @default(now())

  @@unique([country, insuranceType, effectiveFrom])
}

model StaffSalary {
  id            Int            @id @default(autoincrement())
  userId        Int?
  staffRecordId Int?
  baseSalary    Decimal        @db.Decimal(12, 2)
  country       PayrollCountry
  effectiveFrom DateTime
  effectiveTo   DateTime?
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt

  user        User?            @relation("UserSalary", fields: [userId], references: [id])
  staffRecord StaffRecord?     @relation(fields: [staffRecordId], references: [id])
  allowances  StaffAllowance[]
  payrollRuns PayrollRun[]
}

model StaffAllowance {
  id            Int      @id @default(autoincrement())
  staffSalaryId Int
  name          String
  amount        Decimal  @db.Decimal(12, 2)
  taxable       Boolean  @default(true)
  createdAt     DateTime @default(now())

  staffSalary StaffSalary @relation(fields: [staffSalaryId], references: [id])
}

model PayrollRun {
  id                 Int              @id @default(autoincrement())
  staffSalaryId      Int
  month              DateTime
  grossPay           Decimal          @db.Decimal(12, 2)
  totalDeductions    Decimal          @db.Decimal(12, 2)
  netPay             Decimal          @db.Decimal(12, 2)
  status             PayrollRunStatus @default(DRAFT)
  confirmedById      Int?
  confirmedAt        DateTime?
  secondApprovedById Int?
  secondApprovedAt   DateTime?
  isLocked           Boolean          @default(false)
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt

  staffSalary      StaffSalary @relation(fields: [staffSalaryId], references: [id])
  confirmedBy      User?       @relation("PayrollRunConfirmer", fields: [confirmedById], references: [id])
  secondApprovedBy User?       @relation("PayrollRunSecondApprover", fields: [secondApprovedById], references: [id])

  @@unique([staffSalaryId, month])
}

model UserInvite {
  id              Int              @id @default(autoincrement())
  token           String           @unique
  email           String
  role            Role
  coachingRole    CoachingRole?
  frontOfficeRole FrontOfficeRole?
  expiresAt       DateTime
  usedAt          DateTime?
  createdById     Int
  createdAt       DateTime         @default(now())

  createdBy User @relation("InviteSender", fields: [createdById], references: [id])
}

// ─────────────────────────────────────────────
// Feature 16 — 자산관리부서
// ─────────────────────────────────────────────

model SoftwareLicense {
  id          Int       @id @default(autoincrement())
  name        String
  vendor      String
  totalSeats  Int
  usedSeats   Int       @default(0)
  expiresAt   DateTime?
  renewalCost Decimal?  @db.Decimal(12, 2)
  createdById Int
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  createdBy     User           @relation("SoftwareLicenseCreator", fields: [createdById], references: [id])
  assetRequests AssetRequest[]
}

model LedgerEntry {
  id            Int                 @id @default(autoincrement())
  type          LedgerEntryType
  category      LedgerEntryCategory
  amount        Decimal             @db.Decimal(14, 2)
  currency      CurrencyCode        @default(KRW)
  exchangeRate  Decimal             @default(1) @db.Decimal(10, 4)
  amountKrw     Decimal             @db.Decimal(14, 2)
  description   String?
  relatedModule String?
  relatedId     Int?
  isRefund      Boolean             @default(false)
  isAdjustment  Boolean             @default(false)
  accountCodeId Int?
  periodLocked  Boolean             @default(false)
  reversedById  Int?                @unique
  createdById   Int
  createdAt     DateTime            @default(now())

  createdBy         User               @relation("LedgerEntryCreator", fields: [createdById], references: [id])
  reversedBy        LedgerEntry?       @relation("LedgerReversal", fields: [reversedById], references: [id])
  reversal          LedgerEntry?       @relation("LedgerReversal")
  accountCode       AccountCode?       @relation(fields: [accountCodeId], references: [id])
  revenueAdjustment RevenueAdjustment?
}

model LedgerPeriodLock {
  id         Int      @id @default(autoincrement())
  year       Int
  month      Int
  lockedById Int
  lockedAt   DateTime @default(now())

  lockedBy User @relation("PeriodLockCreator", fields: [lockedById], references: [id])

  @@unique([year, month])
}

model WorkPermitAlertLog {
  id       Int      @id @default(autoincrement())
  playerId String
  window   String // "30D" | "60D"
  sentAt   DateTime @default(now())

  player Player @relation(fields: [playerId], references: [id])

  @@unique([playerId, window])
}

model SalesRecord {
  id                 Int               @id @default(autoincrement())
  type               SalesType
  quantity           Int
  unitPrice          Decimal           @db.Decimal(12, 2)
  totalAmount        Decimal           @db.Decimal(12, 2)
  currency           CurrencyCode      @default(KRW)
  saleDate           DateTime
  description        String?
  matchId            Int?
  seatZoneId         Int?
  status             SalesRecordStatus @default(COMPLETED)
  channel            SalesChannel?
  createdById        Int
  createdAt          DateTime          @default(now())
  deletedAt          DateTime?
  updatedAt          DateTime          @updatedAt
  updatedById        Int?
  ticketSerialNumber String?
  refundedFromId     Int?

  createdBy    User          @relation("SalesRecordCreator", fields: [createdById], references: [id])
  updatedBy    User?         @relation("SalesRecordUpdater", fields: [updatedById], references: [id])
  match        Match?        @relation(fields: [matchId], references: [id])
  seatZone     SeatZone?     @relation(fields: [seatZoneId], references: [id])
  refundedFrom SalesRecord?  @relation("RefundChain", fields: [refundedFromId], references: [id])
  refunds      SalesRecord[] @relation("RefundChain")
}

model FacilityInventoryItem {
  id           Int      @id @default(autoincrement())
  name         String
  unit         String
  quantity     Int      @default(0)
  minThreshold Int      @default(10)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model FacilityReservation {
  id           Int          @id @default(autoincrement())
  facilityZone FacilityZone
  title        String
  startTime    DateTime
  endTime      DateTime
  notes        String?
  reservedById Int
  createdAt    DateTime     @default(now())

  reservedBy User @relation("FacilityReservations", fields: [reservedById], references: [id])
}

model Fan {
  id        Int      @id @default(autoincrement())
  name      String
  email     String?  @unique
  phone     String?
  joinedAt  DateTime @default(now())
  createdAt DateTime @default(now())

  memberships FanMembership[]
  purchases   FanPurchase[]
}

enum MembershipTier {
  BRONZE
  SILVER
  GOLD
  PLATINUM
}

model FanMembership {
  id        Int            @id @default(autoincrement())
  fanId     Int
  tier      MembershipTier
  startDate DateTime
  endDate   DateTime
  isActive  Boolean        @default(true)
  createdAt DateTime       @default(now())

  fan Fan @relation(fields: [fanId], references: [id])
}

model FanPurchase {
  id          Int       @id @default(autoincrement())
  fanId       Int
  matchId     Int?
  saleType    SalesType
  quantity    Int
  totalAmount Decimal   @db.Decimal(12, 2)
  seatZone    String?
  purchasedAt DateTime  @default(now())

  fan   Fan    @relation(fields: [fanId], references: [id])
  match Match? @relation(fields: [matchId], references: [id])
}

model SeatZone {
  id        Int    @id @default(autoincrement())
  name      String
  capacity  Int
  unitPrice Int?
  matchId   Int
  soldCount Int    @default(0)

  match        Match         @relation(fields: [matchId], references: [id])
  salesRecords SalesRecord[]
}

model RefreshTokenBlacklist {
  jti       String   @id
  expiresAt DateTime

  @@index([expiresAt])
}

model Agency {
  id             Int      @id @default(autoincrement())
  name           String
  contactName    String?
  phone          String?
  email          String?
  commissionRate Decimal? @db.Decimal(5, 2)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  players          Player[]
  contracts        Contract[]
  transferRequests TransferRequest[]
}

model MonthlyBudgetSnapshot {
  id           Int      @id @default(autoincrement())
  seasonId     Int
  year         Int
  month        Int
  snapshotData Json
  totalBudget  Int
  totalActual  Int
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  season       Season   @relation(fields: [seasonId], references: [id])

  @@unique([seasonId, year, month])
}

model MonthlyOperationsSnapshot {
  id           Int      @id @default(autoincrement())
  seasonId     Int
  year         Int
  month        Int
  snapshotData Json
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  season       Season   @relation(fields: [seasonId], references: [id])

  @@unique([seasonId, year, month])
}

model SquadPlan {
  id          Int      @id @default(autoincrement())
  seasonId    Int      @unique
  formation   String
  slots       Json
  updatedAt   DateTime @updatedAt
  updatedById Int

  season    Season @relation(fields: [seasonId], references: [id])
  updatedBy User   @relation(fields: [updatedById], references: [id])
}

enum AppealStatus {
  PENDING
  ACCEPTED
  REJECTED
}

model AttendanceAppeal {
  id               Int              @id @default(autoincrement())
  trainingResultId Int
  originalStatus   AttendanceStatus
  requestedStatus  AttendanceStatus
  reason           String           @db.Text
  status           AppealStatus     @default(PENDING)
  reviewNote       String?          @db.Text
  createdById      Int
  reviewedById     Int?
  reviewedAt       DateTime?
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt

  trainingResult TrainingResult @relation(fields: [trainingResultId], references: [id])
  createdBy      User           @relation("AppealCreator", fields: [createdById], references: [id])
  reviewedBy     User?          @relation("AppealReviewer", fields: [reviewedById], references: [id])
}

// ─────────────────────────────────────────────
// 계획보고서
// ─────────────────────────────────────────────

enum PlanStatus {
  DRAFT
  REVIEWING
  APPROVED
  REJECTED
}

enum SurveyStatus {
  OPEN
  CLOSED
}

enum SurveyPriority {
  HIGH
  MEDIUM
  LOW
}

model PlanReport {
  id                    Int              @id @default(autoincrement())
  title                 String
  purpose               String
  departmentId          Int
  startDate             DateTime
  endDate               DateTime
  budget                Int
  expectedEffect        String
  risks                 String
  attachments           String[]
  resultDueDate         DateTime
  templateType          PlanTemplateType
  extraFields           Json?
  hasNewStaff           Boolean          @default(false)
  hasContract           Boolean          @default(false)
  hasExternalLease      Boolean          @default(false)
  hasPersonalInfo       Boolean          @default(false)
  isNewBusiness         Boolean          @default(false)
  status                PlanStatus       @default(DRAFT)
  requiredApproverLevel ApproverLevel?
  rejectionReason       String?
  resultContent         String?
  resultSubmittedAt     DateTime?
  submittedAt           DateTime?
  approvedAt            DateTime?
  rejectedAt            DateTime?
  vaultPath             String?
  surveyId              Int?             @unique
  createdById           Int
  approvedById          Int?
  createdAt             DateTime         @default(now())
  updatedAt             DateTime         @updatedAt

  department      Department         @relation(fields: [departmentId], references: [id])
  createdBy       User               @relation("PlanReportCreatedBy", fields: [createdById], references: [id])
  approvedBy      User?              @relation("PlanReportApprovedBy", fields: [approvedById], references: [id])
  reviews         PlanReview[]
  jobPosting      JobPosting?
  survey          HiringNeedsSurvey? @relation(fields: [surveyId], references: [id])
  hiringPlanItems HiringPlanItem[]
}

model HiringNeedsSurvey {
  id          Int          @id @default(autoincrement())
  title       String
  deadlineAt  DateTime
  status      SurveyStatus @default(OPEN)
  createdById Int
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt

  createdBy         User               @relation("SurveyCreatedBy", fields: [createdById], references: [id])
  targetDepartments SurveyTargetDept[]
  responses         SurveyResponse[]
  planReport        PlanReport?
}

model SurveyTargetDept {
  surveyId     Int
  departmentId Int

  survey     HiringNeedsSurvey @relation(fields: [surveyId], references: [id], onDelete: Cascade)
  department Department        @relation("SurveyTargetDept", fields: [departmentId], references: [id])

  @@id([surveyId, departmentId])
}

model SurveyResponse {
  id              Int            @id @default(autoincrement())
  surveyId        Int
  departmentId    Int
  roleTitle       String
  headcount       Int
  quarter         Int?
  priority        SurveyPriority
  estimatedBudget Int?
  reason          String
  submittedById   Int
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt

  survey         HiringNeedsSurvey @relation(fields: [surveyId], references: [id], onDelete: Cascade)
  department     Department        @relation("SurveyResponse", fields: [departmentId], references: [id])
  submittedBy    User              @relation("SurveyResponseSubmitter", fields: [submittedById], references: [id])
  hiringPlanItem HiringPlanItem?

  @@unique([surveyId, departmentId])
  @@index([surveyId])
}

model HiringPlanItem {
  id               Int            @id @default(autoincrement())
  planReportId     Int
  surveyResponseId Int?           @unique
  roleTitle        String
  headcount        Int
  quarter          Int?
  priority         SurveyPriority
  estimatedBudget  Int?
  createdAt        DateTime       @default(now())
  updatedAt        DateTime       @updatedAt

  planReport     PlanReport      @relation(fields: [planReportId], references: [id], onDelete: Cascade)
  surveyResponse SurveyResponse? @relation(fields: [surveyResponseId], references: [id])
  jobPostings    JobPosting[]
}

// ─────────────────────────────────────────────
// 부서 간 교차 검토
// ─────────────────────────────────────────────

enum ReviewStatus {
  PENDING
  CONFIRMED
  REJECTED
}

enum PlanTemplateType {
  GENERAL
  HR
  MARKETING
  GOODS
  SQUAD
  MEDICAL
  IT
}

enum ApproverLevel {
  HEAD
  GM
  ADMIN
}

enum PartnerTier {
  TITLE
  GOLD
  SILVER
  BRONZE
}

enum ClauseType {
  DISCOUNT
  REFUND
}

enum ClauseStatus {
  PENDING
  APPLIED
  WAIVED
}

enum ContactChannel {
  CALL
  EMAIL
  MEETING
  OTHER
}

enum ExposureChannel {
  TV
  SNS
  STADIUM
  PRINT
  DIGITAL
  OTHER
}

enum SettlementStatus {
  DRAFT
  PENDING_FIRST
  FIRST_APPROVED
  APPROVED
  REJECTED
}

model DepartmentReviewerConfig {
  id                   Int      @id @default(autoincrement())
  subjectDepartmentId  Int
  reviewerDepartmentId Int
  createdAt            DateTime @default(now())

  subjectDepartment  Department @relation("SubjectDept", fields: [subjectDepartmentId], references: [id], onDelete: Cascade)
  reviewerDepartment Department @relation("ReviewerDept", fields: [reviewerDepartmentId], references: [id], onDelete: Cascade)

  @@unique([subjectDepartmentId, reviewerDepartmentId])
}

model PlanReview {
  id             Int          @id @default(autoincrement())
  planId         Int
  reviewerDeptId Int
  status         ReviewStatus @default(PENDING)
  comment        String?
  confirmedById  Int?
  confirmedAt    DateTime?
  createdAt      DateTime     @default(now())

  plan         PlanReport @relation(fields: [planId], references: [id], onDelete: Cascade)
  reviewerDept Department @relation("PlanReviewerDept", fields: [reviewerDeptId], references: [id])
  confirmedBy  User?      @relation("PlanReviewConfirmer", fields: [confirmedById], references: [id])

  @@unique([planId, reviewerDeptId])
}

model ReportReview {
  id             Int          @id @default(autoincrement())
  reportId       Int
  reviewerDeptId Int
  status         ReviewStatus @default(PENDING)
  comment        String?
  confirmedById  Int?
  confirmedAt    DateTime?
  createdAt      DateTime     @default(now())

  report       Report     @relation(fields: [reportId], references: [id], onDelete: Cascade)
  reviewerDept Department @relation("ReportReviewerDept", fields: [reviewerDeptId], references: [id])
  confirmedBy  User?      @relation("ReportReviewConfirmer", fields: [confirmedById], references: [id])

  @@unique([reportId, reviewerDeptId])
}

model ReviewRuleSet {
  id               Int                @id @default(autoincrement())
  reportType       ReportType
  reviewerCategory DepartmentCategory
  createdAt        DateTime           @default(now())

  @@unique([reportType, reviewerCategory])
}

model PreventiveSchedule {
  id              Int                 @id @default(autoincrement())
  facilityZone    FacilityZone
  title           String
  description     String?             @db.Text
  intervalDays    Int
  priority        MaintenancePriority
  partnerId       Int?
  lastGeneratedAt DateTime?
  isActive        Boolean             @default(true)
  createdAt       DateTime            @default(now())
  updatedAt       DateTime            @updatedAt

  partner             Partner?             @relation(fields: [partnerId], references: [id])
  maintenanceRequests MaintenanceRequest[]
}

model FacilityAccessLog {
  id        Int          @id @default(autoincrement())
  userId    Int
  zone      FacilityZone
  action    String
  reason    String?
  createdAt DateTime     @default(now())

  user User @relation(fields: [userId], references: [id])
}

model EquipmentDisposalVerification {
  id            Int                        @id @default(autoincrement())
  equipmentId   Int                        @unique
  requestedById Int
  verifiedById  Int?
  verifiedAt    DateTime?
  photoUrl      String?
  checklistOk   Boolean                    @default(false)
  notes         String?                    @db.Text
  status        DisposalVerificationStatus @default(PENDING)
  createdAt     DateTime                   @default(now())

  equipment   EquipmentUnit @relation(fields: [equipmentId], references: [id])
  requestedBy User          @relation("DisposalRequestedBy", fields: [requestedById], references: [id])
  verifiedBy  User?         @relation("DisposalVerifiedBy", fields: [verifiedById], references: [id])
}

model SponsorshipClause {
  id            Int          @id @default(autoincrement())
  sponsorshipId Int
  type          ClauseType
  condition     String       @db.Text
  rate          Decimal?     @db.Decimal(5, 4)
  fixedAmount   Decimal?     @db.Decimal(14, 2)
  status        ClauseStatus @default(PENDING)
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt

  sponsorship     Sponsorship          @relation(fields: [sponsorshipId], references: [id], onDelete: Cascade)
  appliedPayments SponsorshipPayment[]

  @@index([sponsorshipId])
}

model SponsorshipExposureEvent {
  id            Int             @id @default(autoincrement())
  sponsorshipId Int
  channel       ExposureChannel
  occurredAt    DateTime
  exposureCount Int?
  fanReach      Int?
  mediaValue    Decimal?        @db.Decimal(15, 2)
  notes         String?         @db.Text
  createdById   Int
  createdAt     DateTime        @default(now())

  sponsorship Sponsorship @relation(fields: [sponsorshipId], references: [id], onDelete: Cascade)
  createdBy   User        @relation("ExposureEventCreator", fields: [createdById], references: [id])

  @@index([sponsorshipId])
  @@index([createdById])
}

model PartnerContactLog {
  id             Int            @id @default(autoincrement())
  partnerId      Int
  channel        ContactChannel
  contactedAt    DateTime
  actorId        Int
  summary        String         @db.Text
  nextActionDate DateTime?
  nextActionNote String?        @db.Text
  createdAt      DateTime       @default(now())

  partner Partner @relation(fields: [partnerId], references: [id])
  actor   User    @relation("ContactLogActor", fields: [actorId], references: [id])

  @@index([partnerId])
  @@index([actorId])
}

model MonthlySettlementReport {
  id       Int @id @default(autoincrement())
  seasonId Int
  year     Int
  month    Int

  status          SettlementStatus @default(DRAFT)
  rejectionReason String?

  totalRevenue Float @default(0)
  totalExpense Float @default(0)
  netIncome    Float @default(0)

  snapshotJson Json @default("{}")

  note String? @db.Text

  createdById        Int
  firstSubmittedById Int?
  firstSubmittedAt   DateTime?
  firstApproverId    Int?
  firstApprovedAt    DateTime?
  approverId         Int?
  approvedAt         DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  season             Season              @relation(fields: [seasonId], references: [id])
  createdBy          User                @relation("SettlementCreator", fields: [createdById], references: [id])
  firstSubmittedBy   User?               @relation("SettlementFirstSubmitter", fields: [firstSubmittedById], references: [id])
  firstApprover      User?               @relation("SettlementFirstApprover", fields: [firstApproverId], references: [id])
  approver           User?               @relation("SettlementApprover", fields: [approverId], references: [id])
  revenueAdjustments RevenueAdjustment[] @relation("MonthlyAdj")

  @@unique([seasonId, year, month])
}

// ─────────────────────────────────────────────
// 계정코드 & 수익 조정
// ─────────────────────────────────────────────

model AccountCode {
  id        Int             @id @default(autoincrement())
  code      String          @unique
  name      String
  type      AccountCodeType
  createdAt DateTime        @default(now())

  ledgerEntries     LedgerEntry[]
  operatingExpenses OperatingExpense[]
}

model RevenueAdjustment {
  id                Int          @id @default(autoincrement())
  financialReportId Int?
  monthlyReportId   Int?
  field             RevenueField
  delta             Int
  memo              String?
  createdById       Int
  createdAt         DateTime     @default(now())
  ledgerEntryId     Int?         @unique

  financialReport FinancialReport?         @relation("SeasonAdj", fields: [financialReportId], references: [id])
  monthlyReport   MonthlySettlementReport? @relation("MonthlyAdj", fields: [monthlyReportId], references: [id])
  ledgerEntry     LedgerEntry?             @relation(fields: [ledgerEntryId], references: [id])
  createdBy       User                     @relation("AdjCreator", fields: [createdById], references: [id])
}

enum OverrideStatus {
  PENDING
  APPROVED
  REJECTED
}

model FinancialReportRevenueLog {
  id                Int             @id @default(autoincrement())
  financialReportId Int
  field             String
  oldValue          Float
  newValue          Float
  changedById       Int
  createdAt         DateTime        @default(now())
  report            FinancialReport @relation(fields: [financialReportId], references: [id], onDelete: Cascade)
  changedBy         User            @relation("RevenueLogChanger", fields: [changedById], references: [id])
}

// ─────────────────────────────────────────────
// Asset Request Workflow
// ─────────────────────────────────────────────

enum AssetRequestStatus {
  DRAFT
  SUBMITTED
  LEADER_APPROVED
  LEADER_REJECTED
  APPROVED
  REJECTED
  CANCELLED
  FULFILLED
}

enum AssetRequestType {
  SOFTWARE
  HARDWARE
}

enum AssetRequestApprovalStage {
  LEADER // 팀장 (신청자 dept.head)
  DEPT_HEAD // 부서장 (parent dept.head)
}

enum AssetRequestApprovalAction {
  APPROVED
  REJECTED
}

model AssetRequest {
  id           Int                @id @default(autoincrement())
  requesterId  Int
  departmentId Int
  type         AssetRequestType
  status       AssetRequestStatus @default(DRAFT)

  // hybrid payload — one of equipmentItemId/softwareLicenseId/customName required
  equipmentItemId   Int?
  softwareLicenseId Int?
  customName        String?
  customDescription String?

  expenseCategoryId Int
  expectedAmount    Int
  neededBy          DateTime?
  justification     String

  operatingExpenseId Int?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  requester        User                   @relation("AssetRequestRequester", fields: [requesterId], references: [id])
  department       Department             @relation(fields: [departmentId], references: [id])
  expenseCategory  ExpenseCategory        @relation(fields: [expenseCategoryId], references: [id])
  equipmentItem    EquipmentItem?         @relation(fields: [equipmentItemId], references: [id])
  softwareLicense  SoftwareLicense?       @relation(fields: [softwareLicenseId], references: [id])
  operatingExpense OperatingExpense?      @relation(fields: [operatingExpenseId], references: [id])
  approvals        AssetRequestApproval[]

  @@index([requesterId, status])
  @@index([departmentId, status])
  @@index([status, createdAt])
}

model AssetRequestApproval {
  id             Int                        @id @default(autoincrement())
  assetRequestId Int
  stage          AssetRequestApprovalStage
  action         AssetRequestApprovalAction
  reviewerId     Int
  reason         String?
  createdAt      DateTime                   @default(now())

  assetRequest AssetRequest @relation(fields: [assetRequestId], references: [id], onDelete: Cascade)
  reviewer     User         @relation("AssetRequestReviewer", fields: [reviewerId], references: [id])

  @@unique([assetRequestId, stage])
  @@index([assetRequestId, stage])
}
