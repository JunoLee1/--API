import {Router} from "express"

const router = Router()

router.use(
    //유저 CRUD (/api/users)
    //시즌 CRUD (/api/seasons)
    //경기 CRUD (/api/matches)
    //선수 CRUD (/api/players)
)

export default router